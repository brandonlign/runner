# Pinned affine stream-tube source audit

This auxiliary runner-only workflow reconstructs the exact source from commit `2e4fbda1c2f93df3036b3634aa7fa5704c390614` that produced workflow `30878027705` and failed before scientific scoring.

The decoded source must match SHA-256 `e82eef021326cb5b570521064a6c78001cc5d0cac53fdb2e1e966362f3d4c1bb`. The workflow records complete source, static calls and signatures, compilation status, and the source region around the documented `scorer.build_complex_map` interface error.

It downloads no meteor data, reads no labels, forms no windows, computes no score or endpoint, and accesses no confirmation panel or GhostStream. It creates no scientific result.

Any corrected preflight must be separately hashed and may change only the invalid module-interface reference. The physical statistic, coordinates, neighbor rule, affine fit, residual, inputs, years, seeds, calibration counts, comparators, folds, thresholds, gates, and blind interval remain frozen.