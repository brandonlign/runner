# OrbitTrace v49 self-supported joint-slot transfer v1

## Scientific role

Separately frozen exposed-development successor after binding v48 failed 2/4.

The relevant completed evidence is now constrained enough to motivate one architectural change without parameter search:

- #1098 fixes the exact 60-family HDB joint-positive population.
- #1139 shows that recoverable joint-positive families have smaller inherited-component evidence gaps than nonrecoverable families in both exposed years.
- v48 therefore required own quality evidence to be at least as strong as borrowed component evidence before quality priority was allowed; this produced exactly 35 self-supported joint candidates.
- v48 nevertheless moved 225/229 HDB positions because those 35 candidates used their quality ranks in the unrestricted global order, and binding run `31489678337` failed both HDB literature gates.
- v45 separately established the exact joint-slot containment architecture: all 169 non-joint positions can remain immovable while the 60 joint identities are permuted only across the exact v31 positions already occupied by that same fixed set. v45 itself is permanently rejected because it prioritized those slots by inherited component-best evidence; #1136/#1139 subsequently identified why that priority can be harmful.

v49 combines only the surviving constraints. It does **not** revive v45's component-best ordering and does **not** weaken or retune v48's self-support condition.

SonotaCo 2013/2014 remains `EXPOSED_DEVELOPMENT_ONLY`, never external validation.

## Frozen scientific rule

Sugar is exact v31 unchanged.

For HDB:

1. Reconstruct exact v31 order over the fixed 229-family universe.
2. Use the exact #1098 60-family joint-positive set, with no new selector.
3. For every joint-positive family, retain the exact v48 self-support condition:
   `quality_percentile <= component_best_v31_percentile`, equivalently `quality_suppression >= inheritance_gap`.
4. Define the same parameter-free v48 priority key:
   - `quality_rank` if self-supported;
   - otherwise exact `v31_rank`.
5. Let `P` be the exact 60 v31 rank positions occupied by the #1098 joint-positive identities.
6. Sort only those same 60 identities by `(priority_key, exact_v31_rank, family_id)`.
7. Permute the sorted 60 identities one-for-one across the fixed position set `P` in ascending position order.
8. Every one of the 169 non-joint HDB identities must remain at its exact v31 position.
9. No joint family may occupy a formerly non-joint slot and no non-joint family may occupy a joint slot.

This is a slot-containment architecture, not a tuned promotion-strength parameter.

## Truth-blind frozen consequences

Before any v49 SonotaCo outcome is loaded, require all of the following consequences of the fixed truth-blind vectors:

- HDB family count: `229`;
- exact joint-positive count: `60`;
- exact self-supported count: `35`;
- non-joint count: `169`;
- exact v31 HDB order SHA-256: `85ac3f29a443c35b3812cf28c99ef13474fc3e0455458e20b41cec64d942073d`;
- exact v49 HDB order SHA-256: `6948b51c1510add3819fb467118ff829bbe5ffcbd785538abd5a065af3f848c7`;
- exact joint slot set unchanged;
- all non-joint positions unchanged;
- total moved HDB identities: `59`;
- top-9 membership differs from v31 by exactly one incoming and one outgoing family;
- top-11 membership differs from v31 by exactly one incoming and one outgoing family.

The top-9/top-11 cardinalities are frozen truth-blind structural checks only. Their identities and any SonotaCo outcomes do not define the rule.

## Immutable scientific inputs

Use only already-frozen inputs:

- #950 pretruth payload artifact `9074742322`, ZIP SHA-256 `d940fa255804866f14bc34b1d72467d17adddcfb7d82c954ed5a8d1668aa307a`;
- immutable #839 ranker source from run `31344632499`, SHA-256 `dd14e899ac08c4081cfee7d2dac2e54d2f25f78427cc4bee30f30296cd24b990`;
- graph SHA-256 `2f25aa7d9af1df0a350c034b2280a790c3869a7775bf6d7063d0d1e4cc802b25`;
- component SHA-256 `c1b80484502907ad19e0d51d8d48d364f3bf7a27e17576e073fe86ae44f499cd`;
- #1091 run `31456963941`, artifact `9088402091`, result SHA-256 `2edb949dbab2d7cbf6ed5e808e2a049116eb0934b25b356572463b615d730842`;
- #1098 run `31457923695`, artifact `9088724826`, signal SHA-256 `a3bcea66b72003a38cc492ea3b182d92cd20f3d1b94a43acbc8a0cbdd465ed07`;
- #1139 run `31488131546`, artifact `9099927842`, artifact digest `sha256:67960fbd5fd76173da62c6d1823d507c99ee6431862ce56351aa7a194ec81e07`, result SHA-256 `c372b4aac0547198cb6ac4239d604fddc12fdd0f11a930ab39a1e46d01f5e461`, vector SHA-256 `145ceb528e66f924c00c152cf2e5a38a2424ffda8f0a39a7eb80680c1bd5dadd`, vector canonical SHA-256 `0a9eda015ca367697a1dca678a0e8f7d986880fc424a0cbf4573567ab8776672`.

Binding v48 provenance motivating the containment change is run `31489678337`, artifact `9100509632`, artifact digest `sha256:6d30db98546cb61f26d864546f1e809c89ed40783039032e24f664a7a681b251`, result SHA-256 `f663acf4adb21168b834989c803930029a9924b0788af63127ce91a0d3e1b359`. That result is historical motivation only and does not supply identities or parameters to v49.

## Evaluation and binding gate

The complete v49 HDB order must be serialized and hash-verified before v49 outcome truth is loaded. Then reproduce exact v31 controls and evaluate exactly one v49 order.

The first technically valid v49 result is binding.

PASS requires all four existing SonotaCo literature pair gates to win. Any result below 4/4 permanently rejects this exact v49 rule.

## Explicit prohibitions

No alternate self-support inequality, strict-vs-nonstrict inequality search, epsilon/tolerance, numerical inheritance-gap cutoff, quality-suppression cutoff, component threshold, partial promotion, interpolation, coefficient, bonus, cap, weighted fusion, top-k, rank window, budget/year rule, identity exception, boundary rescue list, slot expansion/contraction, alternate slot set, second Pareto layer, component-best priority, v45 retry, v47 retry, global v48 retry, alternate quality order, graph/radius/metric/component change, candidate change, feature/model/k/scaling/diversity/fusion/source-quota search, or post-result second search.

If v49 fails, do not alter the 35-family self-supported consequence, the exact 60-slot set, or the priority key in place.

## Firewall

Protected OrbitTrace solar longitude `20°–55°` remains inaccessible. Do not access OrbitTrace target information or target-region events. Do not access MAARSY or DMS scientifically. Every output must assert these conditions and `SonotaCo = EXPOSED_DEVELOPMENT_ONLY`.