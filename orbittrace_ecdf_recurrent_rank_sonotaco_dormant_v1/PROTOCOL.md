# ECDF recurrent-rank HDBSCAN v1 — dormant SonotaCo parent-superiority protocol

## Dormant status

Frozen before the first technically valid ECDF recurrent-rank GMN 2022/2023 scientific outcome.

This protocol is **dormant** unless the exact frozen GMN successor returns:

`PASS_ECDF_RECURRENT_RANK_HDBSCAN_V1_GMN_DEVELOPMENT`

A GMN FAIL permanently closes this protocol without SonotaCo scientific execution.

SonotaCo 2013/2014 remains **EXPOSED DEVELOPMENT ONLY**. This protocol can never create pristine external-validation evidence.

## Exact successor

The scientific method is exactly ECDF recurrent-rank HDBSCAN v1 as frozen in:

- protocol Git blob `2b8329afbcd16b4ac5afb1f35260ee87e7212bb6`;
- ranker Git blob `89601baf31a133d93409c57961baa57e9203c09f`.

Transport preserves the promoted recurrent-EOM hierarchy, selection, memberships, HDBSCAN settings, annual EOM normalization, and recurrent EOM extraction. On each established label-free SonotaCo comparator universe, the exact selected recurrent-EOM candidates are reordered only by the frozen per-year midrank ECDF rule:

1. descending `min(Q_2013,Q_2014)`;
2. descending `max(Q_2013,Q_2014)`;
3. descending promoted-parent recurrent stability;
4. descending ordinary stability;
5. descending member count;
6. ascending deterministic family ID.

No SonotaCo label, shower identity, comparator output, parent metric, budget result, or literature score may enter candidate construction or ordering. Complete candidate order must be hash-frozen before truth is opened.

## Frozen parent controls and budgets

The promoted recurrent-EOM parent controls are fixed now from its binding 4/4 SonotaCo benchmark:

- Sugar 2013, budget 34: macro-F1 `0.3752906816276458`, recovered F1>0.5 `23`;
- Sugar 2014, budget 46: macro-F1 `0.43773122295664196`, recovered F1>0.5 `24`;
- catalogue-HDBSCAN 2013, budget 11: macro-F1 `0.1914598192215768`, recovered F1>0.5 `11`;
- catalogue-HDBSCAN 2014, budget 9: macro-F1 `0.1685878550176112`, recovered F1>0.5 `9`.

The evaluator remains the exact established one-to-one Hungarian max-F1 evaluation and the exact established truth eligibility semantics. No budget may be changed based on the successor result.

## Binding superiority gate

If and only if the GMN gate passes and this dormant protocol is activated, ECDF recurrent-rank v1 passes SonotaCo parent superiority only if **all four** panels satisfy both:

1. successor macro-F1 is strictly greater than the exact promoted recurrent-EOM parent macro-F1 above;
2. successor recovered F1>0.5 count is at least the exact parent count above.

PASS token:

`PASS_ECDF_RECURRENT_RANK_SONOTACO_PARENT_SUPERIORITY_V1`

Any single-panel failure yields:

`FAIL_ECDF_RECURRENT_RANK_SONOTACO_PARENT_SUPERIORITY_V1`

A failure authorizes no ECDF definition change, raw/ECDF blend, alternate tie rule, panel-specific ranking, year weighting, budget change, or other result-informed rescue.

## Firewall

Until GMN PASS activation, this protocol authorizes **zero SonotaCo scientific access**.

At all times:

- protected `[20.0,55.0]` remains inaccessible;
- no OrbitTrace target information/events;
- no ASFN reuse;
- no AMOS, EFN, MAARSY, or DMS scientific access;
- no result-informed scientific modification.
