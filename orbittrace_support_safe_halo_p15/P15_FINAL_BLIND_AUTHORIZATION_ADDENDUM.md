# P15 final target-free blind-search authorization addendum

## Status

Protocol-only addendum frozen before the authoritative P15 development compatibility result, before any P15 matched Sugar/HDBSCAN truth or superiority result, before any P15 MAARSY external result, and without access to the withheld OrbitTrace reference or any 20°–55° target-region event.

This addendum does not activate Stage A or Stage B. It binds P15 to the already-frozen method-agnostic exact-ID reveal firewall v2 in PR #540 and cannot weaken that firewall after any later result.

## Fixed final discovery/reveal design

The final discovery corpus remains exactly full GMN 2022 and 2023. Only after all authorization prerequisites below pass may the previously inaccessible solar-longitude interval 20°–55° enter Stage A as part of the complete discovery corpus.

Stage A is target-free:

- no withheld OrbitTrace reference may be downloaded, mounted, queried, hashed, or named by the Stage-A process;
- no catalogue shower labels may be used to generate, qualify, score, rank, merge, or expand primary discoveries;
- the exact promoted primary method must run once without parameter search or reranking;
- before any withheld-reference access, Stage A must freeze the complete primary family order, every primary family stable event-ID set and per-year ID set, method/source and prerequisite artifact identities, input identities, and a proof that target reference access was false;
- secondary P15 halo characterization may be serialized for audit but cannot create, delete, qualify, rescue, merge, or reorder any primary family and cannot satisfy final recovery independently.

Stage B remains the exact-ID-only reveal evaluator from PR #540. It may consume only the immutable Stage-A artifact plus a separately withheld reference containing exact stable event IDs/year and provenance. No coordinate, radiant, velocity, orbital, activity, distance, nearest-neighbor, clustering, family-merging, membership-expansion, or reranking operation is allowed after the withheld reference becomes visible.

## Frozen recovery criteria inherited unchanged from PR #540

A qualifying family must contain at least 4 exact withheld target IDs in each of 2022 and 2023 and at least 8 exact target IDs total.

- `FULL_BLIND_RECOVERY`: lowest-rank qualifying primary family has rank <=25.
- `PARTIAL_BLIND_RECOVERY`: if no full recovery, lowest-rank qualifying primary family has rank <=100.
- otherwise `NO_BLIND_RECOVERY`.

These thresholds may not be changed in response to P15 development, matched, external, Stage-A, or Stage-B results.

## Exact P15 method boundary

P15's primary discovery output is the promoted P14 recurrent core with support-safe multiplicity total-order semantics. `EPISODE_SIZE=128` remains immutable; exact scorable cores use frozen multiplicity scores, and exact insufficient-support cores receive no fabricated score and follow every scored core in stable family-ID order. P15 does not change primary core construction, event sets, qualification, or ranking.

P15's sole scientific delta is secondary halo availability: exact P12 characterization runs for directions with at least 128 target-window negatives; an exact `<128` direction contributes zero nonseed proposals and is recorded `CHARACTERIZATION_UNAVAILABLE_INSUFFICIENT_NEGATIVES`. This secondary rule cannot alter Stage-A primary discovery or rank.

Frozen P15 source identities:

- development child SHA256 `22d34131e873825ca60aefbba0b92088f19f57f589fe629bfbd3b7041d160b4b`;
- matched child SHA256 `23d309f6702ed0aa6769381963ea64701ae59c97376a0bae536b527fbc978fe6`.

Any eventual full-corpus Stage-A implementation must be a deterministic transport of the promoted method. If the complete-corpus schema cannot represent an exact frozen operation, Stage A fails closed; no target-aware adaptation is allowed.

## Authorization prerequisites

Stage A remains forbidden unless all of the following have already completed and are independently verified from immutable artifacts:

1. **P15 development compatibility PASS** — exact frozen P15 development source passes its target-excluded 2022/2023 compatibility gate with zero unavailable directions and unchanged inherited P12/P13 scientific identity.
2. **Matched literature superiority PASS** — the frozen P15/P14 primary method passes the already-fixed `SPARSE_STREAM_SUPERIORITY` standard separately against both catalogue HDBSCAN and Sugar in both SonotaCo 2023 and 2025. Broad-only, technical no-result, or any pairwise failure is insufficient.
3. **Independent no-retuning external PASS** — the preregistered MAARSY 2020/2021 route, with the P15 external addendum frozen before its result, returns exact `PASS_P15_MAARSY_2020_2021_NO_RETUNING_EXTERNAL`. Power-inconclusive, architecture-incompatible, scientific failure, or integrity failure does not authorize Stage A.
4. **Stage-A source freeze** — the exact full-corpus P15 transport, output schema, prerequisite-artifact pins, input identities, and proof of target-reference isolation are source-audited before the first 20°–55° discovery row is read.

The matched SonotaCo panel cannot double-count as independent generalization. The previously exposed GMN 2024/2025 panel cannot authorize Stage A.

## Stage-A hard barrier before reveal

Before Stage B can access any withheld target ID, the Stage-A artifact must prove:

- years exactly `[2022, 2023]` and complete full-corpus processing;
- exact promoted method/source identity and exact prerequisite PASS artifact identities;
- complete contiguous primary ranking with no omitted primary family;
- stable event-ID sets for every primary family and each year;
- no catalogue-shower-label use in primary discovery/ranking;
- `target_reference_accessed=false`;
- no post-freeze family generation, membership growth, score recomputation, merging, or reranking is possible in Stage B.

Only after this artifact is durably hash-frozen may the exact-ID evaluator from PR #540 read the withheld reference.

## Claim boundary

A later `FULL_BLIND_RECOVERY` or `PARTIAL_BLIND_RECOVERY` would establish that the completely frozen, target-free, literature-superior, independently generalized method placed a recurrent primary family containing the required exact withheld OrbitTrace membership within the preregistered top-25/top-100 ranking threshold. It would not by itself establish dynamical distinctness from every known stream or completeness of canonical membership.

This addendum authorizes no target access. It must not be edited in response to any P15 development, matched, MAARSY, Stage-A, or Stage-B outcome.
