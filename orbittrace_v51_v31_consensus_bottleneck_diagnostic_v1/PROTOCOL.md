# OrbitTrace v51 v31 consensus-bottleneck diagnostic v1

## Scientific role

This is a post-v50/post-topology **exposed-development mechanism diagnostic only**. It does not define, evaluate, select, or authorize a successor ranking.

The binding state before this diagnostic is:

- v31 remains the parent and reproduces the exact four controls;
- broad auxiliary rerankers have repeatedly degraded the small HDB literature prefixes;
- conservative auxiliary corrections have repeatedly left those prefixes unchanged;
- v50 closed the inherited diversity-penalty endpoint: the exact v31 annual local-geometry margins remained byte-identical while changing only downstream diversity/order construction failed to improve HDB;
- the separately frozen #1149 component-topology diagnostic failed and permanently closed that topology mechanism.

A distinct untested mechanism remains inside exact v31 itself. Its final order is an equal rank-sum of two already-frozen inputs:

1. the strict-OOF local-geometry order after the exact inherited #839 diversity pass; and
2. the immutable exact-v19 order from #950.

Equal rank-sum permits a family ranked very poorly by one input to be compensated by a strong rank from the other input. Before any successor based on agreement/consensus is considered, this diagnostic asks one population-level question:

> **Across the complete fixed 229-family HDB universe, are families that are recoverable in each exposed year ranked strongly by both exact-v31 fusion inputs, as measured by a lower worst normalized input rank?**

Define the sole statistic

`consensus_bottleneck = max(local_rank_percentile, v19_rank_percentile)`

where each percentile is `(rank - 1) / (229 - 1)` using the exact full HDB order. Lower is better and means neither v31 input ranks the family poorly.

This statistic is parameter-free. It is not rank disagreement, rank sum, geometric mean, minimum rank, harmonic rank, Pareto depth, or a new candidate order.

SonotaCo 2013/2014 remains `EXPOSED_DEVELOPMENT_ONLY`, not external validation.

## Immutable parent and inputs

Use only the established v31 execution path and immutable inputs:

1. frozen v31 scientific source blob `917e3cd6f9310ca1282e0efa58ed0924d03ed4da`;
2. immutable #950 payload artifact `9074742322`, ZIP SHA-256 `d940fa255804866f14bc34b1d72467d17adddcfb7d82c954ed5a8d1668aa307a`;
3. immutable #839 ranker source from run `31344632499`, source SHA-256 `dd14e899ac08c4081cfee7d2dac2e54d2f25f78427cc4bee30f30296cd24b990`;
4. immutable exposed SonotaCo truth artifact `9069505548`, ZIP SHA-256 `cdea3297c234b0b3a8f09c2208649c8607bb3e9a9004d299f6dcc18536ebb797`.

The diagnostic must execute exact v31 unchanged while instrumenting only the existing `v19.fusion_orders(local_order, v19_order)` call. The wrapper must record copies of the two input orders and delegate immediately to the frozen function without modifying either input or its returned rank-sum.

Require exact parent reproduction:

- Sugar 2013 `0.2719801488280529 / 16`;
- Sugar 2014 `0.31529041952487225 / 17`;
- HDB 2013 `0.14888037368183737 / 9`;
- HDB 2014 `0.15198123772301594 / 9`.

Require the captured HDB local order SHA-256 to equal the established v31 hash `9898a2ad69f251595b0de4ce5763ffb6641ad27141b6e54f839ebe240eb94595` and the returned HDB fused order SHA-256 to equal exact-v31 `85ac3f29a443c35b3812cf28c99ef13474fc3e0455458e20b41cec64d942073d`.

The exact v19 order must equal the immutable #950 HDB manifest `v19_order` byte-for-byte.

## Frozen diagnostic vector

After exact v31 has reproduced, but before diagnostic recoverability classes are attached, serialize all 229 HDB identities with only:

- family identity;
- exact local-geometry/diversity rank and normalized percentile;
- exact immutable-v19 rank and normalized percentile;
- `consensus_bottleneck = max(local_percentile, v19_percentile)`.

The vector must contain all and only the fixed #950 HDB family universe. No subset, literature budget, boundary identity, component/quality signal, topology class, annual own-family F1, recoverability class, panel result, or successor order may enter the statistic.

Because exact v31 itself is an exposed-development OOF method whose training labels come from SonotaCo, this vector is not claimed to be truth-independent. The scientific firewall is instead that the statistic definition, parent code, complete population, and sole outcome gate are frozen before this diagnostic result, and the vector contains no diagnostic own-family recoverability classification used by the gate.

## Sole truth-aware diagnostic

Using the same established own-family annual-F1 semantics as prior diagnostics:

1. derive each HDB family's fixed best label with the existing `family_truth` implementation over the two exposed years;
2. if positive with a fixed label, compute annual F1 with the existing `annual_f1_for_fixed_label`; otherwise annual F1 is zero;
3. define annual recoverability by the already-established criterion `annual_f1 > 0.5`.

For each year separately, split the complete fixed 229-family population into recoverable and nonrecoverable classes. Both classes must be nonempty.

The sole preregistered direction test is:

`median(consensus_bottleneck | recoverable) < median(consensus_bottleneck | nonrecoverable)`.

PASS requires this strict direction in **both 2013 and 2014**.

Quartiles, minima, and maxima may be reported descriptively for those two already-defined classes but do not enter PASS/FAIL.

No representative-group aggregation, AUC, correlation, regression, p-value, alternate rank statistic, rank-disagreement test, threshold, quantile, top-k, rank window, literature-budget analysis, or panel reranking is authorized.

## Interpretation

PASS supports only this mechanism statement:

> Recoverable HDB families tend to avoid a poor rank in either of the two already-frozen exact-v31 fusion inputs, in both exposed years.

PASS does **not** authorize a worst-rank/minimax successor automatically. Any successor would require a separate truth-blind total-order definition frozen before first evaluation.

FAIL permanently closes this exact consensus-bottleneck mechanism diagnostic. Do not rescue it with rank disagreement, alternative rank normalizations, weighted maxima, soft-max, minimum/mean/geometric ranks, group aggregation, thresholds, or year-specific use.

## Explicit prohibitions

No new candidate order, selector, replacement rule, minimax/worst-rank literature evaluation, Pareto order, fusion weight, rank algebra search, alternate statistic, threshold, quantile, top-k, rank window, budget/year exception, component/quality/topology rescue, boundary identity, oracle family, feature/model/k/metric/scaling/diversity search, source-quota search, or post-result second search.

## Firewall

Protected OrbitTrace solar longitude `20°–55°` remains inaccessible. Do not access OrbitTrace target information or target-region events. Do not access MAARSY or DMS scientifically. Every output must assert these conditions and `SonotaCo = EXPOSED_DEVELOPMENT_ONLY`.
