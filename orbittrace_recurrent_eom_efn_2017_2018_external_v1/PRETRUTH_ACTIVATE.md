# Activate EFN recurrent-EOM pretruth candidate freeze v1

Authorized by `PRETRUTH_CANDIDATE_PROTOCOL.md`, frozen before any EFN pretruth candidate outcome.

Inputs are only the binding Stage-2 2017/2018 canonical geometry artifacts from run `31841984405`. The execution must freeze the complete shared HDBSCAN hierarchy, vanilla/recurrent node selections, memberships, scores, and deterministic ranks before any Stage-3 label access.

No `Shower`, `Object`, orbit field, target information, target-region event/physical value, MAARSY, or DMS access is authorized.
