# Local-contrast recurrence source-mapping audit

This auxiliary runner-only workflow reconstructs the exact candidate source authorized for full Stage-0 and records the complete source plus the static definitions that map public meteor rows into the 4D annual histograms.

Required hashes:

- pinned repository commit: `372ed6aa1ec9da07edd1748ba0f6514bf03c5f81`;
- decoded worst-family source SHA-256: `4384dd0352174e57ca1f93a2c3bd070002f026cef8acace035ba4ec05e577dac`;
- derived local-contrast source SHA-256: `b7589d8d140a37596f19d4993be1e2fdd99a18b8eaa087a02e3c4ce585000071`.

The audit extracts constants, command-line definitions, function signatures, and exact source segments for the input parser, annual histogram construction, kernel definition, local-contrast transformation, recurrence statistic, and Stage-0 driver.

It downloads no meteor data, generates no catalog, calibrates no threshold, injects no stream, computes no detector score or endpoint, and accesses no real-shower label, confirmation panel, catalogue scan, or GhostStream value. It creates no scientific result.

Its sole purpose is implementation mapping for a possible separately frozen real-shower feasibility stage. No source byte or scientific continuation rule may be changed by this audit.