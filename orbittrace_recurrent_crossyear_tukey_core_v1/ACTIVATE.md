Activate the single frozen recurrent cross-year Tukey core v1 binding execution. Protocol and implementation were frozen before this marker. No result-informed rescue or rerun is authorized after a technically valid result.
