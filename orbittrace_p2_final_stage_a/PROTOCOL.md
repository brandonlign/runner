# OrbitTrace P2 final blind Stage-A protocol

## Status

Protocol-only preregistration frozen before any authoritative P2 development verdict, before any P2 matched-literature result, before any MAARSY 2020/2021 event-level value is opened, and before any final target-containing GMN execution.

This freezes deployment only. It does not change P2 science.

## Eligibility

P2 Stage A is dormant unless all three exact upstream scientific gates pass without retuning:

1. exact canonical P2 returns `PASS_CROSSYEAR_TWO_VIEW_MEMBERSHIP_P2_DEVELOPMENT` on target-excluded GMN 2022/2023 under the sole authoritative technical lineage;
2. the already-frozen pairwise P2 Sugar/HDBSCAN comparison returns `BROAD_CATALOGUE_SUPERIORITY` or `SPARSE_STREAM_SUPERIORITY` against both comparator panels;
3. the pristine MAARSY 2020/2021 cross-survey protocol frozen pre-result returns exact `PASS_P2_MAARSY_2020_2021_EXTERNAL_VALIDATION` with all integrity and power conditions satisfied.

Development PASS alone, literature superiority alone, external `INCONCLUSIVE`, external FAIL, or technical/integrity failure cannot authorize Stage A.

GMN 2024/2025 cannot substitute for independent generalization because repository history established prior scientific use of that panel.

## Frozen P2 science

The canonical scientific runtime remains exact P2 v2 SHA-256:

`f19500f6b0dfe481d845af57f3b4d7ec35e678e2191388b7ff4611f8fb2c4eeb`

Its immutable architecture is:

- exact promoted-v8 recurrent seed/core construction and multiplicity order;
- immutable original seeds and unchanged family ranking;
- two cross-year predictive features only: OAS observation-space Mahalanobis distance `d_obs` and exact Southworth–Hawkins distance `d_orb` to the nearest opposite-year immutable seed;
- positives = opposite-year immutable family seeds;
- negatives = every target-year non-seed event in the exact ±5° family window, with at least 128 negatives per family-direction;
- exactly equal family/direction class contribution;
- exact weighted `StandardScaler` plus L2 `LogisticRegression(C=1.0, solver='lbfgs', max_iter=1000, tol=1e-10)`;
- unit background conflict model and strict maximum family responsibility `>0.5`;
- tie resolution by frozen promoted-v8 family rank then family ID only;
- seeds never move; additions never retrain, recenter, refit or seed recursive growth;
- no reranking after membership expansion;
- no parameter, feature, threshold, model or variant search.

The source-audited GMN header-whitespace compatibility layer may be used only as an equivalence-preserving transport repair around this exact runtime. It may normalize BOM/leading/header whitespace and may not alter any named field, event value or P2 rule.

## Final discovery corpus

Stage A applies the already-selected algorithm once to complete geometrically valid GMN 2022 and 2023 rows, now including the previously withheld 20°–55° interval.

The full-region corpus may provide only generic fields required by promoted v8 and P2: stable event identity, time/solar longitude, radiant/speed geometry, and the orbital elements required by exact `d_orb`. Catalogue shower-label values remain unavailable throughout Stage A.

Apart from removing the development-time target-interval exclusion, no row-validity, deduplication, coordinate, speed, orbit, source, temporal, density or window rule may change.

## Hard target-reference firewall

The complete Stage-A catalogue, classifier and membership payload must be immutable before any withheld target-reference value is accessible.

Stage A must not have access to:

- withheld target IDs or year mapping;
- target coordinates, radiant, speed, orbit or activity bounds as target information;
- canonical target members or identity/name;
- historical target rank/recovery from exploratory work;
- any Stage-B reveal result.

The withheld target-reference artifact must not be downloaded, mounted, queried, hashed or selected by the scientific Stage-A process.

Generic orbital values from the full GMN corpus are permitted because exact `d_orb` is a frozen P2 input; they are not a target reference and may not be interpreted using target information.

## Binding Stage-A order

1. verify exact development, literature and MAARSY PASS artifacts before the first full-region GMN scientific row is requested;
2. load complete 2022/2023 generic geometry with shower-label values unavailable;
3. build exact promoted-v8 recurrent seed/core families and freeze the complete multiplicity order;
4. read/decode only the generic orbit fields needed by exact P2 for those full-region event identities, with no target reference available;
5. construct the exact cross-year P2 training set, fit the exact global two-feature model, and SHA-freeze the scaler/classifier before any post-model evaluation;
6. score all exact compatible event/family pairs, resolve conflicts with the exact unit-background responsibility rule, and freeze complete expanded memberships;
7. verify seeds unchanged, no recursive growth/refit, and family order byte-identical to the pre-membership multiplicity order;
8. serialize and SHA-256 freeze the complete Stage-A catalogue and exact-ID projection;
9. only afterward may a separate Stage-B process obtain the already-sealed scrubbed target-ID reference.

No known-shower truth evaluation occurs in final Stage A.

## Required Stage-A artifact

Use schema `orbittrace-final-stage-a-ranked-families-v2` consumed by the method-agnostic exact-ID firewall.

Required top-level fields include:

- `schema`;
- `method_id = "P2-crossyear-two-view-membership"`;
- exact canonical P2 scientific source SHA and exact transport-wrapper identity;
- exact promoted-v8 and D_SH source identities;
- exact prerequisite development/literature/external PASS artifact identities;
- `years = [2022, 2023]`;
- `target_reference_accessed = false`;
- `catalogue_shower_labels_used = false`;
- exact full-region input/provenance identities;
- exact promoted-v8 seed-family/rank pretruth SHA;
- exact P2 model pretruth SHA;
- exact P2 expanded-membership pretruth SHA;
- complete `primary_families` in contiguous unchanged multiplicity order.

Each reveal-facing family record contains only:

- `family_id`;
- `rank`;
- `years`;
- `event_ids_by_year` with exact stable IDs.

Internal provenance may retain P2 model/candidate/conflict diagnostics needed to prove exact execution, but Stage B receives only the exact-ID family projection and immutable provenance hashes.

## Integrity gates before Stage B

Stage B remains closed unless all hold:

- exact canonical P2 scientific source identity;
- only the audited equivalence-preserving header transport, if needed;
- exact promoted-v8 source/core/rank identity;
- exact D_SH source identity;
- exact prerequisite PASS artifacts;
- full 2022/2023 corpus identity/completeness;
- shower-label values unavailable during seeds, training, scoring, memberships and ranking;
- target reference unavailable throughout Stage A;
- exact two-feature model and training rules;
- classifier/model frozen before memberships;
- original seeds preserved;
- no recursive growth/refit;
- family order unchanged before/after expansion;
- complete model, membership and ranking SHA-freezes before reveal;
- contiguous ranks beginning at 1;
- stable event IDs partitioned exactly by year.

A technical failure permits only an equivalence-preserving repair of this same frozen Stage A. It does not authorize another scientific variant.

## Stage B

After a valid Stage A, use only the method-agnostic `orbittrace_final_exact_id_firewall_v2`.

Stage B may receive only a pre-sealed scrubbed withheld reference of exact stable `{id, year}` values. It computes exact set intersections against the already-frozen P2 family memberships. No coordinate/orbit/radiant/activity matching, nearest-neighbor search, family merge, member expansion, reranking or threshold change is allowed after target IDs become visible.

Frozen reveal verdicts remain:

- `FULL_BLIND_RECOVERY`: rank <=25 and >=4 exact withheld IDs in each of 2022 and 2023, >=8 total;
- `PARTIAL_BLIND_RECOVERY`: if no full recovery, rank <=100 with the same per-year/total exact-ID support;
- otherwise `NO_BLIND_RECOVERY`.

## Claim boundary

A qualifying Stage-B result would support a target-free independent-discovery claim for a method selected by target-excluded development, matched literature superiority and genuinely unexposed cross-survey generalization before final target-region application.

It would not by itself prove dynamical distinctness from every known stream or that every historical target member belongs to the recovered family.

This protocol may not be changed because of the P2 development result, literature result, MAARSY result, final Stage-A rank or Stage-B overlap.