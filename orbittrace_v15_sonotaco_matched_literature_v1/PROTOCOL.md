# OrbitTrace v15 matched SonotaCo literature benchmark — source freeze v1

## Evidence status

This is a **frozen exposed-benchmark comparison**, not a new pristine external validation.

SonotaCo 2013/2014 scientific geometry was already exposed when frozen #839 failed its local-episode applicability requirement. The current v15 architecture was developed afterward on target-excluded GMN to repair that architectural incompatibility. Therefore this stage may answer the fixed engineering/scientific question “how does the now-frozen v15 method compare with the already-frozen Sugar and catalogue-HDBSCAN implementations on the exact matched SonotaCo panels?”, but it may not be described as an untouched prospective SonotaCo validation.

The permanent independent external-generalization endpoint remains MAARSY 2022 with MAARSY 2021 as the fixed unlabeled recurrence-support year. This SonotaCo benchmark cannot substitute for MAARSY and cannot authorize target access by itself.

## Activation prerequisites

This source freeze is dormant. Scientific benchmark execution is allowed only if all are already frozen and pass:

1. `PASS_V15_LABEL_FREE_V8_DEPLOYMENT_QUALIFICATION` on target-excluded GMN 2022/2023;
2. `PASS_V15_SONOTACO_2013_2014_ENGINEERING_APPLICABILITY` on the already-exposed base-row pair;
3. exact canonical event adapter, including the corrected scalar MAARSY `vels` transport, remains source-audited;
4. no post-qualification or post-applicability change to v15 proposal/family/scoring/ranking science.

Failure of either prerequisite ends this benchmark route for the current candidate. No benchmark output may be used to alter v15 and rerun SonotaCo as if fresh.

## Fixed pairwise row universes

Reuse exactly the previously frozen final-r4 SonotaCo label-free preparation artifact:

- source run `31354363306`;
- artifact `9050107352`;
- artifact name `orbittrace-final-sonotaco-label-free-preparation-v2`;
- artifact digest recorded by GitHub as `sha256:1296d757b5ea1dd94f9c9077fd769fdc8f00ec06d0881d8548fd1df4608344cc`.

Use only its four existing comparator-specific row files:

- `sugar_2013.json`;
- `sugar_2014.json`;
- `hdbscan_2013.json`;
- `hdbscan_2014.json`.

For each comparator, v15 and that comparator receive the same exact underlying row universe. Cross-comparator denominator mixing is forbidden. The full base-row files are not the scientific input for this matched benchmark.

## Frozen candidate

For each comparator-specific two-year row pair:

1. immediately project every row to the canonical eight-field pretruth record;
2. run the exact promoted label-free-v8 proposal architecture:
   - fixed4 anchored structural proposal graph;
   - first shortlist 64;
   - audit shortlist 128;
   - anchor multiplicity >=2;
   - no label-dependent calibration;
   - no score threshold;
   - top 512 retained quartets per bin;
   - exact frozen component construction;
   - exact frozen recurrent-family linkage radius 1.5;
3. apply the exact promoted-v8 pooled same-year centroid repair;
4. rank the resulting family universe with the merged common v15 nominal-128 multiscale consensus using caps `(128,96,64)` and `K=min(cap,N_local)`, failing only when a required local episode has fewer than four events;
5. freeze exactly one primary catalogue containing family ID, full frozen event-ID membership, and final rank.

No P19, P20, URC learned ranker, halo, rescue membership, survey-conditioned threshold, alternative cap set, or post-output filtering is allowed.

## Frozen comparators

Use the exact already-frozen final comparator sources and adapters:

- Sugar, exact frozen catalogue implementation;
- catalogue HDBSCAN, exact frozen source with the already-audited sklearn-1.4/HDBSCAN-0.8.44 keyword-compatibility shim that changes only `ensure_all_finite` to the equivalent `force_all_finite` keyword.

No comparator parameter, row cut, cluster filter, or postprocessing choice may change.

## Strict pretruth boundary

For both comparator pairs and both years:

1. all pairwise row JSON hashes are fixed;
2. v15 primary output and source manifest are written;
3. comparator primary outputs and source manifests are written;
4. `freeze_pretruth_outputs.py` writes the exact row/output/source hashes for all four panels;
5. only after every freeze manifest exists may any known-shower truth mapping be opened for this v15 benchmark run.

Truth may evaluate frozen outputs only. It may not regenerate a family, rerank a family, filter an output, alter a comparator result, or change a candidate budget.

## Evaluation mathematics

Use the exact candidate-independent final matched-literature evaluator frozen in PR #854, source SHA-256:

`cefcc8900a7b3d083f81148427e9f80e2c7192bb25dd9bb635e6677aa23a555c`.

Do not modify its semantics. In particular:

- known showers require >=4 exact common-row events;
- candidate budget equals the comparator’s frozen year-specific family count;
- v15 is truncated to its first B year-active ranked families, with no padding if it has fewer;
- v15 and comparator are separately matched to truth by deterministic maximum-total-F1 one-to-one assignment;
- size strata are 4–9, 10–24, 25–49, 50–99, 100+;
- broad/sparse point-estimate gates and the 10,000-replicate deterministic stratified bootstrap remain exactly those frozen in #854 / `FINAL_LITERATURE_TEST_POLICY_V1.md`.

The evaluator’s raw mathematical verdict is preserved exactly and may be one of:

- `PASS_FINAL_BROAD_CATALOGUE_SUPERIORITY`;
- `PASS_FINAL_SPARSE_STREAM_SUPERIORITY`;
- `FAIL_FINAL_LITERATURE_SUPERIORITY`;
- `INVALID_FINAL_LITERATURE_TEST_INTEGRITY`.

## Current-project interpretation

Because the SonotaCo panel is no longer pristine for the successor-development chronology, the raw #854 verdict must be wrapped with evidence class `EXPOSED_MATCHED_LITERATURE_BENCHMARK`.

A raw broad/sparse PASS supports the statement that frozen v15 **outperforms the frozen Sugar/HDBSCAN comparators under the exact predeclared matched-benchmark mathematics on SonotaCo 2013/2014**. It does not support the stronger statement that this is an untouched prospective validation.

A raw FAIL is preserved and may not be tuned away on SonotaCo. A raw integrity failure produces no performance interpretation.

No SonotaCo benchmark result changes the requirement for a separately preregistered MAARSY 2021-support/2022-scored external-generalization run.

## Firewall

This source freeze itself accesses no SonotaCo scientific value or truth, no MAARSY event value, no DMS data, and no OrbitTrace target information. The closed 20°–55° target interval remains removed upstream before any retained row reaches candidate/comparator science or truth evaluation.
