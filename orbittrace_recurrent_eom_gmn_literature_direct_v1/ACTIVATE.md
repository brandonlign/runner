Activate exactly one frozen recurrent-EOM direct GMN literature benchmark v1 execution. No parameter tuning, method changes, reranking, threshold changes, or protected-region access are authorized after this marker.

Technical retry only after run 32152452771 failed before geometry/pretruth/truth because the frozen `wavelet_episode_comparator.py` directory was absent from PYTHONPATH. Protocol, comparator definitions, recurrent inputs, evaluation, and gates remain unchanged.
