# Dependency-correction execution trigger

Execute the byte-identical frozen Hissar metadata/interface audit from the dependency-fix parent after installing `pdftotext`. This child changes no interface rule, scientific logic, catalogue access, or target boundary.
