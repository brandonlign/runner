# OrbitTrace v54 full-universe v19-advantage diagnostic v1

## Scientific role

Post-#1157 exposed-development mechanism diagnostic only. This does **not** define, evaluate, select, or authorize a successor ranking.

Binding context:

- exact v31 remains the parent HDB ranking architecture;
- v52 lexicographic minimax fusion is permanently rejected;
- #1157 (`PASS_V31_INTERNAL_V19_SUPPRESSION_DIAGNOSTIC`) established, on the already-frozen #1046 recoverable shower-group representatives, that recoverable-but-missed HDB groups tend to have substantially larger positive immutable-v19 advantage over the local-geometry/diversity leg than surfaced groups in both 2013 and 2014;
- #1157 explicitly requires a complete candidate-level audit before any v19-based successor.

This diagnostic asks one population-level question over **all 229 fixed HDB families**:

> Are annually recoverable families ranked relatively better by immutable v19 than by exact-v31 local geometry/diversity, compared with nonrecoverable families?

For family `i`, using only the exact constituent ranks already frozen in the binding v51 capture, define

`v19_advantage(i) = local_rank_percentile(i) - v19_rank_percentile(i)`.

Positive means immutable v19 ranks the family better than the local-geometry/diversity leg. This statistic is already determined by the exact frozen rank plane; no threshold or candidate selection is introduced.

SonotaCo 2013/2014 remains `EXPOSED_DEVELOPMENT_ONLY`, never external validation.

## Immutable authorizers and inputs

Before current annual recoverability is attached, require:

1. binding #1157 run `31495853601`, artifact `9102914767`, artifact digest `sha256:ff40d3ed14347bce6ea150717961620f082bf15028ecb51999d27c8747c2cd75`, result JSON SHA-256 `165f094fafa0f0f1e78b57dca83fbbf2aeee5d15bdefc9ba4b6f349d495e0aa7`, verdict `PASS_V31_INTERNAL_V19_SUPPRESSION_DIAGNOSTIC`;
2. binding v51 run `31493423814`, artifact `9101972590`, artifact digest `sha256:56258a0be52d83c0d6dbfcffdb9fd9a2c6b73587ba8d92d7bdccdef9729868c9`;
3. v51 capture vector SHA-256 `5f20a8bedb6e7b8d6c06d66e45d5037057a9853ded35a2b360333d6ea5e2c4cc`, canonical SHA-256 `0e13b3f9e6b791a13a3e90d853f8704573b1264dffcb67236e6423491ad70020`;
4. exact HDB constituent-order identities: local SHA `9898a2ad69f251595b0de4ce5763ffb6641ad27141b6e54f839ebe240eb94595`, immutable-v19 SHA `e1e82ad70fb8c575ee7ee269906668931f07cbe3375c15ab84b0717b1f2c85dc`, exact-v31 fused SHA `85ac3f29a443c35b3812cf28c99ef13474fc3e0455458e20b41cec64d942073d`.

The v51 vector must contain exactly 229 unique HDB family identities, complete rank permutations 1..229, and no annual own-family F1 or diagnostic recoverability label.

## Frozen full-universe vector

Before loading the current #950 memberships/truth payload, serialize all 229 HDB families with only:

- `family_id`;
- exact `local_rank`;
- exact `v19_rank`;
- exact `v19_advantage`.

The rank-plane structural consequences are fixed before outcome and must reproduce:

- minimum advantage `-176/228`;
- maximum advantage `160/228`;
- `104` positive advantages;
- `124` negative advantages;
- exactly `1` zero advantage.

These are integrity checks only, not thresholds or selection rules.

The frozen vector must not contain annual F1, recoverability, literature budget, comparator boundary identity, #1157 missed/surfaced identity, v52 substitution identity, component/quality/topology signal, successor order, target information, MAARSY information, or DMS information.

## Sole truth-aware diagnostic

Only after the complete 229-family advantage vector is frozen, restore the same immutable #950 HDB memberships and exposed SonotaCo truth used by the established v31 diagnostics.

For every fixed HDB family, reproduce the established own-family annual-F1 semantics exactly:

1. derive the fixed best label over the two exposed years with the existing `family_truth` implementation;
2. if positive with a fixed label, compute annual F1 with the existing `annual_f1_for_fixed_label`; otherwise annual F1 is zero;
3. annual recoverability remains `annual_f1 > 0.5`.

For each year separately, split all 229 families into recoverable and nonrecoverable classes. Both must be nonempty.

The sole preregistered gate is

`median(v19_advantage | recoverable) > median(v19_advantage | nonrecoverable)`.

PASS requires this strict direction in **both 2013 and 2014**.

Counts, quartiles, minima and maxima may be reported descriptively for the two already-defined classes but do not enter PASS/FAIL.

## Interpretation

PASS supports only the mechanism statement that, across the complete fixed HDB candidate universe, annual-recoverable families tend to receive relatively stronger support from immutable v19 than from the exact-v31 local/diversity leg.

PASS does **not** authorize:

- promoting every positive-advantage family;
- a numerical advantage cutoff;
- rank-gap weighting;
- replacing local rank with v19 rank;
- top-k/budget replacement;
- any successor total order.

Any successor still requires a separate truth-blind order definition frozen before its first literature evaluation.

FAIL permanently closes this exact full-universe median v19-advantage mechanism diagnostic. Do not rescue it with a sign-only test, alternative rank normalization, absolute gap, ratio/log transform, group aggregation, threshold, quantile, year-specific use, or post-result second search.

## Explicit prohibitions

No candidate total order, selector, replacement, promotion, sign threshold, magnitude threshold, quantile, top-k, rank window, budget/year exception, subgroup rule, fusion weight, rank algebra, local-v19 interpolation, component/quality/topology rescue, boundary identity, oracle family, feature/model/k/metric/scaling/diversity search, literature-panel evaluation, or post-result second search.

## Firewall

Protected OrbitTrace solar longitude `20°–55°` remains inaccessible. Do not access OrbitTrace target information or target-region events. Do not access MAARSY or DMS scientifically. Every output must assert these conditions and `SonotaCo = EXPOSED_DEVELOPMENT_ONLY`.
