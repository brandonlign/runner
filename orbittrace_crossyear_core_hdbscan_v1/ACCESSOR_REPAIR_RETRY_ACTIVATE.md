# Activate clean cross-year-core GMN retry after accessor-only technical repair

This activation occurs only after:

- first GMN attempt `31847359367` was preserved as a technical no-result with no prelabel/result file and no truth evaluation;
- the scientific protocol, Boruvka adapter, original development runner, recurrent-EOM implementation, parent runner, metrics, and gate remained byte-identical;
- zero-data accessor audit `31848115820` passed under exact `hdbscan==0.8.43`;
- audit artifact `9236605098`, digest `sha256:d386f7dc14b5c248e48b74a1fa9008bd43e868f7d88dd6a2b18d5fdae05b8929`, proved official `SingleLinkageTree.to_numpy()`, estimator `_single_linkage_tree`, and compatibility `_raw_tree` are bit-identical;
- `ACCESSOR_REPAIR_EXECUTION_FREEZE.json` was frozen before this activation.

The clean retry executes the unchanged frozen scientific runner only through the audited compatibility wrapper. The first technically valid GMN endpoint remains binding. No outcome-informed change, rescue, threshold, parameter, feature, representation, hierarchy, ranking, metric, or gate modification is authorized.

Protected `[20°,55°]`, OrbitTrace target information/events, SonotaCo, AMOS, EFN, MAARSY, and DMS remain inaccessible to methodology selection.
