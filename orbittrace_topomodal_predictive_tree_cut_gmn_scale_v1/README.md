# Predictive Tree Cut v1 GMN scale/generalization endpoint

Status: **closed pretruth structural failure**.

The unchanged SonotaCo-developed Predictive Tree Cut v1 selector was prospectively tested on the exact target-excluded GMN scale-stress construction. After an engineering-only runtime repair, binding run `32035358123` reached the frozen capacity gate and failed on the first coarse panel: selected `24` candidates versus recurrent-EOM equal-budget `K=29` (`d=128`, bucket `0`, year `2022`).

The evaluation job was skipped. No truth-metric comparison was run and no rescue is authorized. See `PROTOCOL.md` and the repository-level result record for exact provenance and interpretation limits.
