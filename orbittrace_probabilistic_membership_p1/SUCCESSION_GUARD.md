# P1 succession guard

This is an execution-authorization addendum only. It does not change the frozen P1 scientific source, parameters, membership model, evaluator, gates, or claim boundary.

## Single authorized predecessor outcome

P1 may execute **only** after the exact repaired/fanout-equivalent v3-primary catalogue-v6 target-excluded development artifact returns the scientific verdict:

`FAIL_V3_PRIMARY_CATALOGUE_V6_DEVELOPMENT`

This restriction is frozen before the current fanout-v2 v6 result is known.

The previously prepared route from `LITERATURE_NO_SUPERIORITY` is superseded pre-result and is no longer authorized. If v6 passes development but the frozen matched Sugar/HDBSCAN adjudication later returns `NO_LITERATURE_SUPERIORITY`, the separately frozen v6-core probabilistic-membership C1 successor (`agent/orbittrace-v6-core-probabilistic-membership-c1-freeze`) has priority because it preserves the scientifically viable v6 detector/core universe. P1 must not be used to replace those viable v6 cores merely because the literature benchmark is insufficient.

P1 may not execute after:

- a technical or integrity failure;
- a cancelled/no-result v6 run;
- a v6 development PASS;
- any literature result, including `NO_LITERATURE_SUPERIORITY`;
- a manually asserted prerequisite.

## Activation file

Activation is an execution-only child containing exactly one file:

`orbittrace_probabilistic_membership_p1/RUN_GUARDED.md`

with exactly two lines:

1. `EXECUTE_FROZEN_P1_AFTER_V6_SCIENTIFIC_FAIL`
2. the numeric GitHub Actions workflow run ID containing the exact fanout-v2 v6 scientific-failure artifact

The workflow itself downloads and verifies artifact `orbittrace-v6-fanout-v2-development`, requires the exact frozen v6 scientific FAIL and intact target-excluded configuration, and only then reconstructs the exact frozen P1 source SHA-256 `e7847e067bab8d07038c998359ccbf0ca6e2ccf257f27f27f4aef999cc7a0508`.

No P1 scientific choice is changed by this succession restriction.