# P18 final target-free blind-search authorization

## Status

Frozen before any P17/P18 matched truth, MAARSY result, Stage-A target-region event access, or withheld OrbitTrace reference access.

This protocol binds P18 to the already-frozen method-agnostic exact-ID reveal firewall. It does not activate Stage A or Stage B.

## Stage A — target-free discovery

Only after exact P18 matched sparse-superiority PASS and exact `PASS_P18_MAARSY_2020_2021_NO_RETUNING_EXTERNAL` may Stage A read the previously inaccessible 20°–55° interval as part of the complete full-GMN 2022/2023 discovery corpus.

Stage A must:
- have no access to the withheld OrbitTrace exact-ID reference;
- use no catalogue labels to generate, qualify, score, rank, merge, or expand primary discoveries;
- execute the exact frozen P18 method once with no parameter search or reranking;
- freeze the complete contiguous primary family order, every primary family stable event-ID set and per-year ID set, input/source/prerequisite artifact identities, P17 closure ledger, P18 reported-membership hashes, and `target_reference_accessed=false` before reveal;
- keep family existence/rank determined only by the P14/P17 primary core/order; P18 halo membership may be serialized but cannot create/delete/merge/qualify/rerank a primary family or independently satisfy blind recovery.

## Stage B — exact-ID reveal only

After the complete Stage-A artifact is durably hash-frozen, Stage B may read only a separately withheld reference containing stable target event IDs/year and provenance. No coordinate, radiant, velocity, orbit, activity, distance, nearest-neighbor, clustering, family merging, membership growth, score recomputation, or reranking is allowed after the reference becomes visible.

A qualifying primary family must contain at least 4 exact withheld target IDs in each of 2022 and 2023 and at least 8 exact target IDs total.

- `FULL_BLIND_RECOVERY`: lowest-rank qualifying primary family has rank <=25.
- `PARTIAL_BLIND_RECOVERY`: if no full recovery, lowest-rank qualifying primary family has rank <=100.
- otherwise `NO_BLIND_RECOVERY`.

These thresholds are immutable and may not change in response to any matched, external, Stage-A, or Stage-B outcome.

## Authorization prerequisites

Stage A remains forbidden unless immutable artifacts independently establish:
1. P17 artifact-only development vacuity/nonregression and exact pretruth closure implementation, with no post-result tolerance rescue;
2. P18 canonical development identity (exact P16 core↔halo identity on the 226-family development universe and P17 branch vacuous there);
3. `PASS_P18_MATCHED_SPARSE_SUPERIORITY_BOTH_COMPARATORS_BOTH_YEARS` under the fixed HDBSCAN/Sugar 2023/2025 evaluator/gates;
4. `PASS_P18_MAARSY_2020_2021_NO_RETUNING_EXTERNAL` under the preregistered external panel/transport/gates;
5. a separately source-audited complete-corpus P18 Stage-A transport proving target-reference isolation and no post-freeze analytical degree of freedom.

The matched SonotaCo panel cannot count as independent generalization, and previously exposed development/validation panels cannot substitute for the preregistered MAARSY requirement.

This protocol authorizes no target access by itself.
