# P18 core-rank / P17-halo reported-membership freeze

## Status

Frozen before any P17/P18 matched comparator cluster value, known-shower truth value, matched F1/superiority result, external result, or OrbitTrace target access.

P18 is the P16 output architecture carried onto the scientifically completed P17 secondary-halo transport. It introduces no detector, family, score, threshold, covariance, distance, proposal, or ranking rule.

## Exact architecture

- Family existence and qualification remain the immutable P14/P17 recurrent core.
- Primary order remains the immutable P14 support-safe multiplicity order; `EPISODE_SIZE=128` and no-fabricated-score semantics are unchanged.
- Secondary membership is generated only by exact P17: exact P12/P15 halo science for support-eligible directions, with P15 `<128` directions unavailable and P17 reciprocal-support closure keeping P9 fail-closed when the reciprocal direction is unavailable.
- P18 reports the exact already-frozen P17 halo membership for each already-discovered stable family ID.
- P18 cannot create/delete/merge/qualify/rerank a family, seed additional growth, propose a new meteor, fill an unavailable direction, or change P17 closure semantics.
- Before truth, each P18 checkpoint must prove one-to-one core↔halo family IDs, core event IDs are a subset of halo IDs, and reported additions equal exactly `halo − core`.
- The complete core order/hash, P17 closure ledger/hash, P17 halo hash, P18 reported-membership hash, and core↔halo correspondence hash freeze before comparator cluster values or known-shower truth.

## Development identity

Canonical target-excluded development already fixes both ingredients independently:

- P16 canonical output-architecture PASS: workflow `31329487091`, artifact `9042496657`, digest `sha256:1d470dcd2034e8b693d80117380f544f43cf6c6d581574a356dc11a04bb91040`; 226 one-to-one core/halo families and exactly 17,238 already-frozen halo additions.
- P17 development branch is unreachable on canonical development because the admissible artifact-only P15 development evidence has 452/452 support-eligible directions, minimum negative count 2,197, and zero unavailable directions.

Therefore P18 development is an identity composition: exact P16 reported-halo membership plus a P17 branch that is vacuous on development. No fresh development rerun or post-result numerical tolerance may be used to promote P18.

Canonical identities remain:
- core pretruth SHA `12e6635085c77c8c705fe225e67811c659e98bf7cd1047649ec2b8d593261b3c`;
- halo membership SHA `f158ebfa3a9a3c8006a7c81cbf0b47f7307aa7f2537e8046621b08037230cca3`.

## Matched benchmark

P18 inherits the exact frozen SonotaCo 2023/2025 HDBSCAN/Sugar row universes, truth eligibility, matching, F1, false-positive burden, information-parity rules, and firewall. The evaluator mathematics and gates do not change.

Promotion requires, separately against both catalogue HDBSCAN and Sugar in both years:
- 4–9-member mean F1 >= comparator +0.10;
- combined 4–24-member mean F1 >= comparator +0.10;
- overall macro F1 no more than 0.10 below comparator;
- retain >=80% of comparator count of showers with F1>0.5;
- all common-universe/integrity/firewall gates pass.

P18 checkpoints must be transformed only from fully frozen P17 pretruth checkpoints. No P17/P18 truth result may influence the transformation.

## External and final-blind boundary

Matched PASS authorizes only the separately preregistered no-retuning MAARSY 2020/2021 external validation. It never authorizes target access by itself.

Only exact matched sparse-superiority PASS plus exact no-retuning external PASS can authorize the separately frozen full-GMN-2022/2023 Stage-A target-free search and exact-ID-only Stage-B reveal. Final blind recovery remains based on the primary family rank, not halo-only membership.
