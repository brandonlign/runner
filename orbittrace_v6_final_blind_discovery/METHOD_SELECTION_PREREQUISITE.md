# Binding method-selection prerequisite for ordinary-v6 blind deployment

This addendum is frozen before any authoritative ordinary-v6 strict-semantic development verdict, before any v6-LF development verdict, before either method's matched-literature result, and before any target-containing Stage A execution.

It is binding on `PROTOCOL.md` and resolves the project-level method-selection hierarchy frozen separately in PR #573.

## Additional mandatory authorization

The ordinary repaired-v6 target-containing Stage A is authorized **only** if all requirements already listed in `PROTOCOL.md` pass **and** the fully label-free v6-LF architecture has first produced a genuine frozen scientific development no-go:

`FAIL_V6_LABEL_FREE_ALL_EVENT_NULL_NO_GO`

from the exact frozen all-event-null method, with all v6-LF integrity gates true and at least one frozen scientific development gate false.

A v6-LF technical failure, timeout, import error, missing artifact, parser/infrastructure error, cancellation, or power/integrity failure is not a scientific no-go and cannot authorize ordinary v6.

If v6-LF passes development, ordinary v6 is not the promoted method regardless of its own development metrics. Ordinary-v6 matched literature, external transfer, and final target deployment remain dormant. v6-LF owns the primary literature/generalization/final-deployment route under its separately frozen protocols.

If v6-LF passes development but later fails matched literature, ordinary v6 still cannot be substituted after observing that result. Succession is to the separately pre-frozen v6-LF-core conservative membership successor C1-LF, not to ordinary v6.

Only the branch `v6-LF scientific development FAIL + canonical strict-semantic ordinary-v6 development PASS` can proceed to ordinary-v6 literature/generalization. Only after those ordinary-v6 downstream gates also pass may this final blind protocol activate.

## Canonical ordinary-v6 provenance

The ordinary-v6 development PASS used here must come from the canonical strict semantic-replay result lineage frozen before verdict. At the time of this addendum, the authorized execution is GitHub Actions run `31285583731`, artifact `orbittrace-v6-semantic-replay-development`. A broader replay result from original fanout run `31282128101` is non-authoritative for scientific routing.

A future replacement is allowed only if a separately frozen pre-result integrity rule establishes exact scientific equivalence and explicitly supersedes this canonical lineage without changing detector science.

## No change to target science

This addendum changes no GMN corpus, target firewall, score, calibration rule, window, proposal cap, v3 threshold, membership rule, component rule, recurrence rule, ranking, rescue separation, reveal matching, rank threshold, or overlap criterion. It only removes an otherwise available post-result method-selection degree of freedom.

No OrbitTrace target coordinates, target IDs, target identity, prior target rank, target-containing family, or excluded-interval event is accessed here.
