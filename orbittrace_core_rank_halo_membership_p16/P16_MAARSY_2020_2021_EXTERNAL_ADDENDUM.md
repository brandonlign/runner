# P16 no-retuning external-validation addendum — MAARSY 2020/2021

## Status

Protocol-only addendum frozen after canonical target-excluded P16 development identity PASS (`31329487091`, artifact `9042496657`, digest `sha256:1d470dcd2034e8b693d80117380f544f43cf6c6d581574a356dc11a04bb91040`) and **before any P15/P16 matched comparator cluster value, known-shower truth value, matched F1/superiority outcome, MAARSY event value, or OrbitTrace target access**.

This addendum cannot execute external data. It applies only if the exact frozen P16 architecture passes the already-frozen matched sparse-superiority requirement against both catalogue HDBSCAN and Sugar in both SonotaCo 2023 and 2025.

## Inherited external panel, transport, power floor, and gates

P16 inherits the pre-result MAARSY 2020/2021 reservation and P15 support-availability addendum without changing any external choice or threshold:

- years remain exactly MAARSY 2020 and 2021;
- exact preaccess transport remains original blob `f563ca63e169e6ddfe846219d8f00384f3451797`, adapter blob `bbf71f190ca128e3e24f71900fefbd1472cdacb6`, source-audit run `31325401462`, generated transport SHA256 `3e0bcc3db805f60310ec9b01d6d48f76b29544780119c05e71fc225c04e3fe48`;
- fixed 10°-bin identity cap remains 10,000 events per bin;
- solar longitude 20°–55° is excluded before scientific values are retained;
- primary recurrent-core family construction and support-safe multiplicity rank remain exact P14/P15 semantics with immutable `EPISODE_SIZE=128`, no fabricated score, and unscorable cores after every scored core;
- secondary halo directions retain exact P15 `MIN_DIRECTION_NEGATIVES=128` fail-closed semantics: a `<128` direction contributes zero nonseed proposals and is recorded `CHARACTERIZATION_UNAVAILABLE_INSUFFICIENT_NEGATIVES`; no padding, resampling, borrowing, threshold relaxation, or observable substitution;
- power eligibility remains at least 5 truth labels with >=4 usable events in each year and at least 5 recurrent cores;
- `POWER_INCONCLUSIVE_MAARSY_2020_2021` remains distinct from scientific failure;
- for a power-eligible panel, exact label-free halo macro-F1 must exceed immutable core macro-F1 by >=0.08;
- when the externally large-shower subset is nonempty, exact halo recall must be >=1.5x immutable core recall and exact halo precision must be >=0.85;
- all inherited core discovery, integrity, information-parity, and firewall gates remain mandatory.

No P16 result may change the years, transport, power floor, truth eligibility, family generation, rank, halo proposal rule, thresholds, or target authorization.

## Sole P16 output-architecture delta

P16 does not recompute a detector or a halo. It carries the development-frozen separation to MAARSY:

1. family existence/qualification is determined only by the immutable recurrent core;
2. family rank is determined only by the immutable support-safe multiplicity order;
3. the reported/evaluator-facing membership for each already-discovered family is exactly the frozen label-free P15/P12 halo that belongs to the same stable family ID;
4. core IDs must be a subset of the reported halo IDs;
5. every reported halo addition must already be an exact P15/P12 halo addition; P16 may not propose a new event;
6. halo membership cannot create/delete/merge/qualify/rerank a family or seed additional growth;
7. the exact core↔halo correspondence and reported-membership hashes must freeze before any MAARSY truth value is indexed.

Thus the inherited external effect-size gates are evaluated exactly as preregistered: immutable core is the discovery/reference membership output, exact halo is P16's reported membership output. The gate is not changed merely because P16 promotes the halo to the reported catalogue membership.

## Development and matched prerequisites

External execution is authorized only if immutable artifacts establish all of the following:

- P16 canonical development verdict `PASS_P16_CORE_RANK_HALO_MEMBERSHIP_CANONICAL_DEVELOPMENT_IDENTITY` from run `31329487091`, artifact `9042496657`;
- family count 226, exact P13 core identity `12e6635085c77c8c705fe225e67811c659e98bf7cd1047649ec2b8d593261b3c`, exact P12 halo membership identity `f158ebfa3a9a3c8006a7c81cbf0b47f7307aa7f2537e8046621b08037230cca3`, and exactly 17,238 already-frozen halo additions;
- no new detector score, threshold, family, or member proposal in P16;
- exact P15 matched halo source SHA256 `23d309f6702ed0aa6769381963ea64701ae59c97376a0bae536b527fbc978fe6` under the frozen P15 support-safe rule;
- both HDBSCAN and Sugar matched checkpoints were frozen before truth, then transformed deterministically to P16 while preserving the exact primary order;
- the fixed matched evaluator establishes `PASS_P16_MATCHED_SPARSE_SUPERIORITY_BOTH_COMPARATORS_BOTH_YEARS`.

Broad-only superiority, an underpowered/incomplete matched panel, or any pairwise sparse failure does not authorize external validation.

## External verdict and target boundary

For a power-eligible panel, the exact passing verdict is `PASS_P16_MAARSY_2020_2021_NO_RETUNING_EXTERNAL`; otherwise it is `FAIL_P16_MAARSY_2020_2021_NO_RETUNING_EXTERNAL`. Underpowered remains `POWER_INCONCLUSIVE_MAARSY_2020_2021`; an exact frozen scientific observable/operation that cannot be represented from the MAARSY transport remains `ARCHITECTURE_INCOMPATIBLE`.

Only exact P16 matched sparse-superiority PASS plus exact `PASS_P16_MAARSY_2020_2021_NO_RETUNING_EXTERNAL` can satisfy the methodology/generalization prerequisites for the separately frozen exact-ID final blind search. This addendum itself authorizes no target access and must not be edited in response to any matched, MAARSY, or target outcome.
