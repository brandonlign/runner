# Recurrent local-BIC HDBSCAN v1 — binding GMN result

## 🔴 NEGATIVE — frozen promotion gate FAIL

The first technically valid target-excluded GMN 2022+2023 endpoint completed successfully in workflow run `31926875888` at execution head `eec4ae19ec2d08bf4618f9895d3b782f4e1389ff`.

Binding artifact: `9258177023`  
Artifact digest: `sha256:09a252b6428f7cbcfc8a98f02b753cbb01640e65e9db48eb9cc080446ebe10db`  
Binding prelabel SHA-256: `fd400043e1356b4db7d85c1d97b5384f24f8643d0854af4fb559462c7a2ce355`.

Exact verdict:

`FAIL_RECURRENT_LOCAL_BIC_HDBSCAN_V1_GMN_DEVELOPMENT`

All source/runtime pins, the zero-truth scale-invariance synthetic audit, exact promoted-parent reproduction, pretruth freeze, scientific endpoint, frozen-gate enforcement, provenance preservation and firewall assertions completed successfully. This is a binding scientific outcome, not a technical no-result.

## Mechanism activity

The lower-support hierarchy and local-BIC extraction were strongly active:

- promoted recurrent-EOM candidates: **2,097**;
- local-BIC candidates: **4,371**;
- successor membership universe differed from the parent;
- local-BIC condensed-tree cluster nodes evaluated: **18,530**;
- nodes with support from both years: **18,150**;
- positive-BIC nodes: **13,760**;
- median local-BIC quality: `8.23209547149958`.

This substantially expanded the recoverable catalogue rather than producing a near-identity rerank.

## Exact annual metrics

### 2022

Recurrent-EOM -> recurrent local-BIC:

- recovered@25: `22 -> 22`;
- recovered@50: `45 -> 45`;
- recovered@100: **`89 -> 88`** — regression;
- recovered@500: `193 -> 189` *(reporting only)*;
- top-100 dominant precision: **`0.7856486012780942 -> 0.7757743421467856`** — regression;
- MRR: **`0.022498269587309373 -> 0.020861590669211817`** — regression;
- qualified matches: **`236 -> 259`**;
- median top-500 fragmentation: `1.0 -> 1.0`.

### 2023

- recovered@25: `23 -> 23`;
- recovered@50: `46 -> 46`;
- recovered@100: **`89 -> 90`** — strict gain;
- recovered@500: `192 -> 188` *(reporting only)*;
- top-100 dominant precision: **`0.7867680236864514 -> 0.7855601091686247`** — slight regression;
- MRR: **`0.0220239288966045 -> 0.020260635327200405`** — regression;
- qualified matches: **`244 -> 267`**;
- median top-500 fragmentation: `1.0 -> 1.0`.

## Why the frozen gate fails

The successor does achieve the required strict @100 gain in one year (`2023: 89 -> 90`) and does not regress @50 or fragmentation. However:

- 2022 @100 regresses `89 -> 88`;
- top-100 precision regresses in both years;
- MRR regresses in both years.

The exact preregistered gate therefore fails.

## Scientific interpretation

The result is informative despite the formal failure. Lowering the hierarchy support floor exposed materially more catalogue-recoverable shower structure: qualified matches increased by **+23 in each year**, and 2023 @100 improved by one. This supports the pre-outcome hypothesis that the promoted 10/10 HDBSCAN hierarchy can hide recoverable low-support structure.

However, the exact local-BIC quality does not rank/filter that enlarged hierarchy well enough at the fixed early budgets. It roughly doubles the number of selected candidates, and the additional catalogue coverage is accompanied by worse early reciprocal ranking and slightly lower top-100 purity. Thus the unresolved problem is no longer simply candidate existence; it is statistically controlling/ranking the extra low-support branches without discarding their recovered coverage.

This exact local-BIC architecture is permanently closed. Per the frozen protocol, do not rescue it by changing intrinsic dimension, `8/4` support, logarithmic persistence, BIC penalty, common-evidence formula, score clipping, rank order, metric, feature scale or promotion gate.

A future successor must be a genuinely distinct architecture motivated independently of this endpoint. In particular, a statistically calibrated multi-scale significance/null mechanism is distinct from this uncalibrated BIC-quality rule and may be considered only under a separately frozen protocol.

## Firewall

The binding result records:

- `blind_exclusion=[20.0,55.0]`;
- `target_information_access=false`;
- `target_region_events_accessed=false`;
- `sonotaco_2013_2014_access=false`;
- `amos_scientific_access=false`;
- `maarsy_scientific_access=false`;
- `dms_scientific_access=false`;
- `post_result_parameter_search=false`.
