# Recurrent-Pareto inactivity router v1 — binding result

## Status

**STRUCTURAL_PRETRUTH_FAIL_RECURRENT_PARETO_INACTIVITY_ROUTER_V1 — CLOSED**

The exact frozen panel-level inactivity router is closed before truth. This is a structural pretruth failure, not a scientific truth-stage result and not a workflow/plumbing no-result.

## Frozen science

- science commit: `e662a516c9996b2cdd73d5ab295202feff51a69a`
- protocol blob: `7f71e013df1ae9c4afc09b5a2852f49720872086`
- frozen pretruth builder blob: `bd91eece4da711fb68649c57f0cbe5611ea08180`
- frozen truth evaluator blob: `236b5eeef7d74bdfa132153f3b0fde5ccc7716bf`

The frozen routing rule was:

- if recurrent-EOM is structurally ACTIVE versus ordinary EOM, preserve exact recurrent-EOM candidates/order;
- if recurrent-EOM is structurally INACTIVE, invoke the exact already-frozen recurrent-TopoModal Pareto-prominence construction/order;
- no sample-size threshold, tolerance, fallback, exception, or truth-informed switch.

The preregistered activity map routed d=64 bucket 1 and bucket 2 to Pareto, while d=64 buckets 0 and 3 stayed exact recurrent-EOM.

## Execution history

### Original execution — technical no-result

- run: `32157337069`
- result: workflow/pretruth failure before truth
- failure: `sparse recurrent identity mismatch (128, 0)`
- cause: the frozen router recomputed a truncated 20-character family membership hash while the sealed scale-stress artifact stored full 64-character SHA-256 membership hashes.
- truth job: skipped

This was an interface/serialization mismatch only and therefore was not a binding scientific or structural result.

### Repair 01 — identity-adapter-only execution

- run: `32182808641`
- repair workflow provenance commit: `23ea03fdd24901664851ed6814cca63b37bfcdd3`
- exact science checkout remained: `e662a516c9996b2cdd73d5ab295202feff51a69a`
- repair scope: replace only the scale-stress membership-hash serialization with the full SHA-256 pipe-joined membership identity used by the sealed scale-stress artifact; no candidate membership, ranking, route rule, threshold, truth gate, or protected-data rule changed.
- immutable prerequisite artifact digests were rechecked before execution.

Repair 01 cleared the identity mismatch and proceeded through reconstruction of the target-excluded GMN 2022/2023 catalogue. It then failed inside the frozen scientific pretruth builder at the exact unique-parent condition:

`RuntimeError: d64 inactive child overlaps multiple recurrent parents b=1`

The failing check is the frozen condition that every support-resolved TopoModal child on an INACTIVE d=64 panel overlap at most one recurrent parent before Pareto construction.

The truth job was skipped. No repaired pretruth artifact or truth artifact was uploaded because failure occurred before pretruth sealing.

## Why this is binding

The frozen protocol explicitly required:

1. d=64 buckets 1 and 2 to satisfy exact unique-parent corroboration without rescue;
2. failure of any pretruth authorization condition to close this exact router before truth;
3. no fallback if an INACTIVE panel cannot construct the exact original Pareto catalogue;
4. no post-result activity tolerance, node-overlap threshold, sample-size threshold, fallback hierarchy, alternate Pareto correspondence, parent-set resolver, route-specific exception, or other rescue.

Repair 01 reached that preregistered structural condition with the serialization mismatch removed. Therefore the exact router fails its pretruth authorization and is permanently closed.

## Interpretation

The inactivity-routing idea does not solve the d=64 portability problem under the exact frozen Pareto correspondence. Avoiding Pareto on d=64 bucket 0 was insufficient: an intended INACTIVE route, bucket 1, also contains at least one support-resolved TopoModal child that overlaps multiple recurrent parents.

This strengthens the existing conclusion that the main barrier to dense/small-denominator TopoModal transfer is the correspondence topology itself, not merely whether recurrent-EOM happens to be active in a panel. Any future method that handles many-to-many TopoModal↔Recurrent correspondence would have to be a genuinely new preregistered mechanism, not a repair or continuation of this router.

This result does **not** alter the frozen recurrent-EOM method, the fixed-scale TopoModal results on panels where they were already valid, the matched-capacity literature-comparator results, or the prior pristine external-validation record.

## Firewall

Truth remained unopened in both executions. The binding structural failure occurred in zero-label pretruth.

No new scientific access was authorized to:

- the protected GMN solar-longitude interval `[20°,55°]`;
- OrbitTrace target identity/events/orbital information;
- SonotaCo;
- ASFN/EFN event-level data;
- AMOS;
- MAARSY;
- DMS.

No result-informed parameter search or rescue is authorized from this failure.
