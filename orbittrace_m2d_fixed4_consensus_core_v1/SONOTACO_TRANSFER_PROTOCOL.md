# M2D x fixed4 consensus core v1 — dormant SonotaCo transfer preregistration

## Activation

This transfer is dormant unless the exact target-excluded GMN development method on PR #1398 returns:

`PASS_M2D_FIXED4_CONSENSUS_CORE_V1_GMN_DEVELOPMENT`

A GMN FAIL permanently disables this transfer. This file is frozen while the GMN run is still unresolved and therefore cannot be changed using its result.

## Frozen method

Transfer the exact dual-output architecture from `PROTOCOL.md` without changing any scientific rule:

- primary discovery output remains the exact baseline support-resolved TopoModal + M2D candidate membership and rank;
- secondary consensus core is computed within each parent candidate and year using the exact fixed4 anchor distance to all other same-envelope/year events;
- each anchor selects exactly three nearest others;
- a canonical quartet is corroborated only when selected by at least two distinct anchors;
- core is the union of events in all corroborated quartets;
- no fixed4 calibration threshold, score cutoff, p-value, distance threshold, family link, best-component choice, expansion, reranking or fallback.

The exact GMN source blobs frozen before the development outcome are:

- protocol `ed62d5149cde2c2070eb21520e77503e719ba9e0`;
- pretruth builder `140f21736ea6615fe111e02d91eaa99b19422da7`;
- truth evaluator `da56bcac1fc78ade58b4323179a5a980f3b1566e`.

Any future SonotaCo implementation must reproduce those scientific semantics exactly. Transport-only adapters may rename the already-known row fields but cannot change the metric, neighbor count, multiplicity or core rule.

## Frozen SonotaCo parent and inputs

The parent discovery catalogue is the already-frozen baseline internal-mass SonotaCo development output, not the later support-pruned refinement:

- ranked-pretruth file Git blob: `e558023e9bb00f75e34a83b84e578012176ce721`;
- ranked-pretruth SHA-256: `9be0e77d650cabd94eccf0623f005705bb86e84793c76190b0065621631f2ecd`;
- candidate count: `888`;
- common-universe size: `29,246` events;
- years: `2013, 2014`.

Label-free row transport remains the exact previously frozen common-universe preparation artifact from run `31354363306` (`orbittrace-final-sonotaco-label-free-preparation-v2`). Known-shower truth/evaluation transport remains the immutable artifact from run `31405109267` (`orbittrace-v15-exposed-matched-sonotaco-literature-result-v1`, artifact digest `sha256:cdea3297c234b0b3a8f09c2208649c8607bb3e9a9004d299f6dcc18536ebb797`).

No support-pruned SonotaCo ranking/result may enter this transfer.

## Firewall order

If activated:

1. verify the exact GMN PASS artifact and all source identities above;
2. download only the label-free 29,246-row common universe and exact 888-candidate baseline M2D ranked pretruth;
3. generate every 2013/2014 fixed4 consensus core using the frozen rule;
4. freeze the complete parent-to-core mapping and SHA-256 while known-shower truth is absent from the workspace;
5. only then open the immutable exposed SonotaCo truth/evaluator package;
6. evaluate the same-parent/same-shower paired utility exactly once;
7. preserve the first technically valid PASS/FAIL.

Candidate generation, ranking, fixed4 consensus membership and all scientific gates must complete before truth access. A technical failure before a scientific result may receive only provenance/transport repair.

## Frozen utility gates

The SonotaCo transfer copies the exact GMN same-discovery thresholds; none may be changed from the GMN outcome:

For the set of baseline-parent assignments with parent F1 > 0.5 across the established SonotaCo evaluation budgets, all are required:

1. at least 20 paired parent-recovered assignments;
2. nonempty core fraction >= 0.75;
3. mean core precision >= 0.80;
4. mean core precision strictly higher than parent precision;
5. mean core F1 >= 0.75 x parent mean F1;
6. among nonempty cores, precision is no lower than parent precision on at least 50% of assignments.

The primary parent M2D ranking/discovery metrics must reproduce their exact frozen baseline values before the core result is accepted. Rematched-core scores may be reported diagnostically but cannot rescue a failed same-parent gate.

## Claim boundary

A PASS is a no-retuning cross-survey transfer of the **characterization layer** on an exposed SonotaCo benchmark. It does not turn SonotaCo into pristine external validation. It authorizes one later exact frozen application of the core rule to the already-blind baseline M2D OrbitTrace candidate, with reveal restricted to exact canonical event-ID intersection after the core membership is frozen.

A FAIL closes the consensus-core architecture. No SonotaCo-informed repair, threshold change or replacement core is authorized from that result.