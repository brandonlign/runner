name: OrbitTrace P13 matched launcher v1 superseded

on:
  workflow_dispatch:

jobs:
  superseded:
    if: ${{ false }}
    runs-on: ubuntu-22.04
    steps:
      - run: echo 'Superseded after technical no-result 31305824131; use launcher v2.'
