name: OrbitTrace pooled-year centroid v8 development

on:
  pull_request:
    branches:
      - agent/orbittrace-pooled-year-centroid-v8
    paths:
      - 'orbittrace_pooled_year_centroid_v8/RUN.md'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  develop:
    runs-on: ubuntu-22.04
    steps:
      - name: Check out frozen v8 development parent
        uses: actions/checkout@v4
        with:
          ref: agent/orbittrace-pooled-year-centroid-v8

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: ghoststream_fixed4_application/requirements.txt

      - name: Install exact scientific dependencies
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip -r ghoststream_fixed4_application/requirements.txt
          python -m pip install --no-deps gmn-python-api==0.0.13

      - name: Enforce frozen source and predecessor artifacts before development access
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p input/v3 input/v6 input/centroid output
          test "$(git hash-object orbittrace_pooled_year_centroid_v8/PROTOCOL.md)" = "8b0a1dc8565a702af6188d42dcebe6b1b71002b6"
          test "$(git hash-object orbittrace_pooled_year_centroid_v8/run_development.py)" = "f248df78e1258b132b41aecca6a985a5eb782654"
          test "$(git hash-object orbittrace_label_free_sparse_support_v6/run_development.py)" = "7995fc6b75d1fd51eb4b304ace39db28a5a1e876"
          test "$(git hash-object orbittrace_label_free_sparse_support_v6/PROTOCOL.md)" = "95feefca6670933795bcc65dd5d9a101a8c4e540"

          python orbittrace_wavelet_catalogue_v3/audit_development_source.py
          test "$(cat output/development_source_sha256.txt)" = "ef3e69317af59fdac7a030edc77f742fc4772473d7f16b719b5d804cd4117f51"

          git fetch --no-tags --depth=1 origin d8258581af143308495bd97bedcc142abbbd951a
          git show FETCH_HEAD:orbittrace_multi_anchor_energy_v3/multi_anchor_energy_v3.py > input/v3/multi_anchor_energy_v3.py
          test "$(git hash-object input/v3/multi_anchor_energy_v3.py)" = "2ba4835db23f8f623cdd28d0a4e6113b7954ecb2"
          test "$(git hash-object orbittrace_wavelet_catalogue_v3/wavelet_episode_comparator.py)" = "493fcc7f2d2cc75ee35acf17e142e7ce7c1e03e8"

          curl -L --fail --retry 3 -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/actions/artifacts/9005846925/zip" -o input/v6.zip
          echo "3c636b05cbfc88c6d6b2b8289b309412174b0025c305ae2f2532678927b2232b  input/v6.zip" | sha256sum -c -
          unzip -q input/v6.zip -d input/v6

          curl -L --fail --retry 3 -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/actions/artifacts/9009091129/zip" -o input/centroid.zip
          echo "a1faacf51e1f4ca2a3a92b20e27d1f07f00e1d32ab33993916379ff36acea062  input/centroid.zip" | sha256sum -c -
          unzip -q input/centroid.zip -d input/centroid

          python - <<'PY'
          import json
          from pathlib import Path
          v=json.loads(Path('input/v6/label_free_sparse_support_v6_development.json').read_text())
          assert v['verdict']=='PASS_LABEL_FREE_SPARSE_SUPPORT_V6_DEVELOPMENT'
          assert v['family_count']==226
          assert v['metrics']['multiplicity']['recovered_at_100']==60
          assert v['metrics']['label_free_persistence']['recovered_at_100']==59
          assert v['metrics']['label_free_persistence']['qualified_matches']==95
          c=json.loads(Path('input/centroid/component_centroid_source_audit.json').read_text())
          assert c['verdict']=='PASS_COMPONENT_CENTROID_SOURCE_AUDIT'
          assert c['catalogue_access'] is False and c['scientific_value_access'] is False
          assert c['target_information_access'] is False
          assert c['support_source_sha256']=='fa18a19c08c6824c66606cbd92095dc3605cbcc30f17a468c9e525e7c6ff4a62'
          PY

          python -m py_compile \
            input/v3/multi_anchor_energy_v3.py \
            orbittrace_wavelet_catalogue_v3/wavelet_episode_comparator.py \
            orbittrace_sparse_support_multiplicity_v5/run_holdout.py \
            orbittrace_label_free_sparse_support_v6/run_development.py \
            orbittrace_pooled_year_centroid_v8/run_development.py
          rm -rf output/*
          echo PASS_POOLED_YEAR_CENTROID_V8_PREDATA_GUARDS

      - name: Run one-shot target-excluded pooled-year-centroid v8 development
        env:
          PYTHONPATH: input/v3:orbittrace_wavelet_catalogue_v3:.
        run: |
          set -euo pipefail
          python orbittrace_wavelet_catalogue_v3/audit_development_source.py
          python orbittrace_pooled_year_centroid_v8/run_development.py \
            --support-source-parts orbittrace_fixed4_support_wrapper_development/source_parts \
            --candidate-payload sonotaco_fixed4_final_development/candidate.py.gz.b64 \
            --baseline-payload real_shower_meta_stage0/run_baseline_ceiling.py.gz.b64 \
            --scorer-parts mondrian_clique_development/source_parts_v2 \
            --source-audit-json output/wavelet_catalogue_v3_source_audit.json \
            --v6-result-json input/v6/label_free_sparse_support_v6_development.json \
            --centroid-audit-json input/centroid/component_centroid_source_audit.json \
            --output output
          cat output/POOLED_YEAR_CENTROID_V8_DEVELOPMENT.md

      - name: Enforce result integrity and claim boundary
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          from pathlib import Path
          r=json.loads(Path('output/pooled_year_centroid_v8_development.json').read_text())
          assert r['verdict'] in {'PASS_POOLED_YEAR_CENTROID_V8_DEVELOPMENT','FAIL_POOLED_YEAR_CENTROID_V8_DEVELOPMENT'}
          c=r['configuration']
          assert c['years']==[2022,2023]
          assert c['blind_exclusion']==[20.0,55.0]
          assert c['family_builder']=='exact passed-v6 connected recurrent family graph'
          assert c['centroid_statistic']=={'sol':'circular_mean_deg','sun_lon':'circular_mean_deg','ecl_lat':'median','vg':'median'}
          assert c['family_link_radius']==1.5 and c['episode_size']==128 and c['top_k']==100
          assert c['no_threshold_search'] is True and c['no_radius_search'] is True
          assert c['no_cap_search'] is True and c['no_weight_search'] is True
          assert c['no_pooling_rule_search'] is True and c['no_rrf'] is True
          assert r['family_count']==226
          assert r['centroid_repair_diagnostics']['non_centroid_family_structure_unchanged'] is True
          assert r['centroid_repair_diagnostics']['families_with_duplicate_same_year_components']>0
          assert r['centroid_repair_diagnostics']['changed_duplicate_year_centroids']>0
          assert 'No OrbitTrace target information' in r['claim_boundary']
          print('ORBITTRACE_POOLED_YEAR_CENTROID_V8_RESULT_BEGIN')
          print(json.dumps(r,indent=2,sort_keys=True))
          print('ORBITTRACE_POOLED_YEAR_CENTROID_V8_RESULT_END')
          PY

      - name: Record immutable provenance
        if: always()
        run: |
          set +e
          git rev-parse HEAD > output/development_commit.txt
          sha256sum \
            orbittrace_pooled_year_centroid_v8/PROTOCOL.md \
            orbittrace_pooled_year_centroid_v8/run_development.py \
            orbittrace_label_free_sparse_support_v6/run_development.py \
            orbittrace_sparse_support_multiplicity_v5/run_holdout.py \
            input/v3/multi_anchor_energy_v3.py \
            orbittrace_wavelet_catalogue_v3/wavelet_episode_comparator.py > output/source_sha256.txt 2>/dev/null || true
          python --version > output/python_version.txt
          python -m pip freeze > output/environment.txt

      - name: Upload v8 development artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: orbittrace-pooled-year-centroid-v8-development
          path: output
          if-no-files-found: error
          retention-days: 90
