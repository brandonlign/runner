name: ISEF XMM DR14 obslist schema probe
on:
  push:
    branches: [main]
    paths:
      - 'isef/xmm_dr14_obslist_schema_probe.py'
      - '.github/workflows/isef-xmm-dr14-obslist-schema-probe.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: pip install astropy
      - run: python isef/xmm_dr14_obslist_schema_probe.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: isef-xmm-dr14-obslist-schema-probe
          path: results/xmm_dr14_obslist_schema_probe.json
          if-no-files-found: error
