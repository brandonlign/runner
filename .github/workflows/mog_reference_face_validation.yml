name: Mog reference-face validation

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/mog_reference_face_validation.yml
      - mog_reference_face_validation/**
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: mog-reference-face-validation
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: mog_reference_face_validation
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/mog-reference-face-validation

      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Extract Apache-2.0 GNM head template
        run: |
          python -m pip install --disable-pip-version-check numpy
          python scripts/extract_gnm_head.py

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install validation dependencies
        run: npm install --no-audit --no-fund

      - name: Assemble exact private-branch renderer mirror
        run: npm run assemble

      - name: Verify geometry and rendering invariants
        run: npm test

      - name: Typecheck isolated Next app
        run: npm run typecheck

      - name: Production build
        run: npm run build

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Render all four views
        run: npm run test:e2e

      - name: Upload visual evidence and licensed mesh sources
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mog-reference-face-verified-renderings
          path: |
            mog_reference_face_validation/artifacts
            mog_reference_face_validation/generated/canonical_face_model.json
            mog_reference_face_validation/generated/gnm_head_skin_template.npz
            mog_reference_face_validation/generated/gnm_head_skin_report.json
          if-no-files-found: warn
          retention-days: 30
