name: ISEF XMM high stack likelihood
on:
  push:
    branches: [main]
    paths:
      - 'isef/xmm_high_stack_likelihood.py'
      - '.github/workflows/isef-xmm-high-stack-likelihood.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: pip install astropy numpy scipy
      - run: python isef/xmm_high_stack_likelihood.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: isef-xmm-high-stack-likelihood
          path: results/xmm_high_stack_likelihood.json
          if-no-files-found: error
