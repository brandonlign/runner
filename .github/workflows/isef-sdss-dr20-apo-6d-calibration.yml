name: ISEF SDSS DR20 APO anonymous 6D calibration
on:
  push:
    paths:
      - '.github/workflows/isef-sdss-dr20-apo-6d-calibration.yml'
  workflow_dispatch:
permissions:
  contents: read
  issues: write
jobs:
  calibrate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install 'numpy>=2.2' 'astropy>=7.1,<8'
      - run: timeout 1200s python isef/sdss_dr20_apo_6d_calibration.py
      - uses: actions/upload-artifact@v4
        with:
          name: isef-sdss-dr20-apo-6d-calibration
          path: results/sdss_dr20_apo_6d_calibration.json
      - if: always()
        env: {GH_TOKEN: '${{ github.token }}'}
        run: |
          { echo '## SDSS DR20 anonymous APO 6D calibration'; echo '```json'; cat results/sdss_dr20_apo_6d_calibration.json 2>/dev/null || true; echo '```'; } >/tmp/r.md
          gh issue comment 1447 --repo "$GITHUB_REPOSITORY" --body-file /tmp/r.md
