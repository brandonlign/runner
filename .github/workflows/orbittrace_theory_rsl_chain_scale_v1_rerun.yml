name: OrbitTrace theory-scaled RSL v1 corrected execution

on:
  push:
    branches:
      - agent/orbittrace-theory-rsl-chain-scale-v1
    paths:
      - '.github/workflows/orbittrace_theory_rsl_chain_scale_v1_rerun.yml'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

env:
  PYTHONUNBUFFERED: '1'
  OMP_NUM_THREADS: '1'
  OPENBLAS_NUM_THREADS: '1'
  MKL_NUM_THREADS: '1'
  NUMEXPR_NUM_THREADS: '1'

jobs:
  diagnostic:
    runs-on: ubuntu-22.04
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4

      - uses: actions/checkout@v4
        with:
          repository: brandonlign/runner
          ref: c9d6c44704013ba0c9430100e98a29a56b453304
          path: frozen-v8

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: frozen-v8/ghoststream_fixed4_application/requirements.txt

      - name: Install exact runtime and verify frozen science plus correction wrapper
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip -r frozen-v8/ghoststream_fixed4_application/requirements.txt
          python -m pip install --no-deps gmn-python-api==0.0.13
          python -m pip install 'numpy==2.1.3' 'scipy==1.14.1' 'scikit-learn==1.7.1' 'hdbscan==0.8.43'
          mkdir -p input/v3 input/v8 input/ranker output

          git -C frozen-v8 fetch --no-tags --depth=1 origin d8258581af143308495bd97bedcc142abbbd951a
          git -C frozen-v8 show FETCH_HEAD:orbittrace_multi_anchor_energy_v3/multi_anchor_energy_v3.py > input/v3/multi_anchor_energy_v3.py
          test "$(git hash-object input/v3/multi_anchor_energy_v3.py)" = '2ba4835db23f8f623cdd28d0a4e6113b7954ecb2'

          rm -f /tmp/run_wavelet_catalogue_v3_development.py
          (
            cd frozen-v8
            python orbittrace_wavelet_catalogue_v3/audit_development_source.py
            test "$(cat output/development_source_sha256.txt)" = 'ef3e69317af59fdac7a030edc77f742fc4772473d7f16b719b5d804cd4117f51'
          )
          test "$(sha256sum /tmp/run_wavelet_catalogue_v3_development.py | cut -d' ' -f1)" = 'ef3e69317af59fdac7a030edc77f742fc4772473d7f16b719b5d804cd4117f51'

          test "$(git hash-object orbittrace_recurrent_eom_hdbscan_v1/run_development.py)" = 'fdc4f3f6e037014aadfcc3ce41b7344aa0a80b2c'
          test "$(git hash-object orbittrace_theory_rsl_chain_scale_v1/PROTOCOL.md)" = '4d8ec4f67a41ccada0c9c1c776604e783b1c4bf1'
          test "$(git hash-object orbittrace_theory_rsl_chain_scale_v1/run_diagnostic.py)" = '2c28267bd1a76581ad7803920a68d7d2e6e8f217'
          test "$(git hash-object orbittrace_theory_rsl_chain_scale_v1/run_diagnostic_v2.py)" = '110e44b11957b126cb6c7ebd108dc7fa562b5e9e'
          python -m py_compile orbittrace_theory_rsl_chain_scale_v1/run_diagnostic.py orbittrace_theory_rsl_chain_scale_v1/run_diagnostic_v2.py

      - uses: actions/download-artifact@v4
        with:
          name: orbittrace-active-urc-ranker-source-export-v1
          path: input/ranker
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
          run-id: 31344632499

      - name: Verify frozen GMN runtime utility
        run: |
          set -euo pipefail
          test "$(sha256sum input/ranker/run_urc_union_ranker.py | cut -d' ' -f1)" = 'dd14e899ac08c4081cfee7d2dac2e54d2f25f78427cc4bee30f30296cd24b990'

      - uses: actions/download-artifact@v4
        with:
          name: orbittrace-pooled-year-centroid-v8-development
          path: input/v8
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
          run-id: 31217916558

      - name: Verify frozen GMN support artifact
        run: |
          set -euo pipefail
          test "$(sha256sum input/v8/pooled_year_centroid_v8_development.json | cut -d' ' -f1)" = 'fa8f52cf046ced499a378cc6b7d04c52ef92bf0fa3f801049211d190f1c3919b'

      - name: Run first technically valid zero-label RSL structural diagnostic
        env:
          PYTHONPATH: input/ranker:.:orbittrace_recurrent_eom_hdbscan_v1:input/v3:frozen-v8/orbittrace_wavelet_catalogue_v3:frozen-v8
        run: |
          set -euo pipefail
          python -u orbittrace_theory_rsl_chain_scale_v1/run_diagnostic_v2.py \
            --parent-runner orbittrace_recurrent_eom_hdbscan_v1/run_development.py \
            --quality-source input/ranker/run_urc_union_ranker.py \
            --support-source-parts frozen-v8/orbittrace_fixed4_support_wrapper_development/source_parts \
            --candidate-payload frozen-v8/sonotaco_fixed4_final_development/candidate.py.gz.b64 \
            --baseline-payload frozen-v8/real_shower_meta_stage0/run_baseline_ceiling.py.gz.b64 \
            --scorer-parts frozen-v8/mondrian_clique_development/source_parts_v2 \
            --v8-result-json input/v8/pooled_year_centroid_v8_development.json \
            --output output

      - name: Enforce unchanged frozen diagnostic contract
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          r=json.load(open('output/THEORY_RSL_CHAIN_SCALE_V1.json'))
          assert r['scientific_role']=='ZERO_LABEL_STRUCTURAL_VIABILITY_DIAGNOSTIC_ONLY'
          assert r['interpretation'] in {'SUPPORTS_THEORY_RSL_CHAIN_AND_SCALE_HYPOTHESIS','REFUTES_THEORY_RSL_CHAIN_AND_SCALE_HYPOTHESIS'}
          assert r['rsl']['dimension']==6
          assert r['rsl']['k_rule']=='ceil(6*ln(n))'
          assert len(r['subsets'])==8
          assert r['blind_exclusion']==[20.0,55.0]
          assert r['target_information_access'] is False
          assert r['target_region_events_accessed'] is False
          assert r['shower_truth_used'] is False
          assert r['sonotaco_2013_2014_access'] is False
          assert r['asfn_event_level_access'] is False
          assert r['efn_event_level_access'] is False
          assert r['amos_scientific_access'] is False
          assert r['maarsy_scientific_access'] is False
          assert r['dms_scientific_access'] is False
          assert r['method_parameter_selection_from_result'] is False
          print(r['interpretation'])
          print(json.dumps(r['summary'],indent=2,sort_keys=True))
          PY

      - name: Preserve exact provenance
        if: always()
        run: |
          set +e
          sha256sum \
            orbittrace_theory_rsl_chain_scale_v1/PROTOCOL.md \
            orbittrace_theory_rsl_chain_scale_v1/run_diagnostic.py \
            orbittrace_theory_rsl_chain_scale_v1/run_diagnostic_v2.py \
            orbittrace_theory_rsl_chain_scale_v1/ENGINEERING_CORRECTION_V1.md \
            orbittrace_theory_rsl_chain_scale_v1/ENGINEERING_CORRECTION_V2.md \
            orbittrace_recurrent_eom_hdbscan_v1/run_development.py \
            input/ranker/run_urc_union_ranker.py \
            input/v8/pooled_year_centroid_v8_development.json \
            output/THEORY_RSL_CHAIN_SCALE_V1.json \
            > output/source_input_output_sha256.txt 2>/dev/null || true
          git rev-parse HEAD > output/execution_commit.txt
          python --version > output/python_version.txt
          python -m pip freeze > output/environment.txt

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: orbittrace-theory-rsl-chain-scale-v1-corrected
          path: output
          if-no-files-found: error
          retention-days: 90
