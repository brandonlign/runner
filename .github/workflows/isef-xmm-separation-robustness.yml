name: ISEF XMM separation robustness
on:
  push:
    branches: [main]
    paths:
      - 'isef/xmm_separation_robustness.py'
      - '.github/workflows/isef-xmm-separation-robustness.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: pip install astropy numpy scipy
      - run: python isef/xmm_separation_robustness.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: isef-xmm-separation-robustness
          path: results/xmm_separation_robustness.json
          if-no-files-found: error
