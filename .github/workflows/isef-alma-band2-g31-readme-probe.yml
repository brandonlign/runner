name: ISEF ALMA Band 2 G31 README probe
on:
  push:
    paths:
      - 'isef/alma_band2_g31_readme_probe.py'
      - '.github/workflows/isef-alma-band2-g31-readme-probe.yml'
  workflow_dispatch:
permissions:
  contents: read
  issues: write
jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - run: timeout 120s python isef/alma_band2_g31_readme_probe.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: isef-alma-band2-g31-readme-probe
          path: results/alma_band2_g31_readme_probe.json
      - if: always()
        env: {GH_TOKEN: '${{ github.token }}'}
        run: |
          { echo '## ALMA Band 2 G31 README probe'; echo '```json'; cat results/alma_band2_g31_readme_probe.json 2>/dev/null || true; echo '```'; } >/tmp/r.md
          gh issue comment 1447 --repo "$GITHUB_REPOSITORY" --body-file /tmp/r.md
