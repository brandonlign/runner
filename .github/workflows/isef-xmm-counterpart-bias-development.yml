name: ISEF XMM counterpart bias development
on:
  push:
    branches: [main]
    paths:
      - 'isef/xmm_reprocessing_counterpart_bias_development.py'
      - '.github/workflows/isef-xmm-counterpart-bias-development.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  analyze:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: pip install astropy numpy scipy
      - run: python isef/xmm_reprocessing_counterpart_bias_development.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: isef-xmm-counterpart-bias-development
          path: results/xmm_reprocessing_counterpart_bias_development.json
          if-no-files-found: error
