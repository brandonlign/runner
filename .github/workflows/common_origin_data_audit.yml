name: Common-origin Stage-0 source verification

on:
  push:
    branches:
      - agent/common-origin-stage0-pilot
    paths:
      - common_origin_pilot/**
      - .github/workflows/common_origin_data_audit.yml
  pull_request:
    branches:
      - main
    paths:
      - common_origin_pilot/**
      - .github/workflows/common_origin_data_audit.yml
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/common-origin-stage0-pilot
          fetch-depth: 0
      - name: Print decoded source hashes
        run: |
          expected_parts=(
            
          )
          : > /tmp/whole.py
          index=0
          for part in common_origin_pilot/stage0_parts/part*.b64; do
            decoded="/tmp/part${index}"
            base64 --decode --ignore-garbage "$part" > "$decoded"
            echo "part=$index size=$(wc -c < "$decoded") expected=${expected_parts[$index]} actual=$(sha256sum "$decoded" | cut -d' ' -f1)"
            cat "$decoded" >> /tmp/whole.py
            index=$((index+1))
          done
          echo "whole_size=$(wc -c < /tmp/whole.py) whole_actual=$(sha256sum /tmp/whole.py | cut -d' ' -f1)"
