name: ISEF XMM Chandra Swift local-null robustness
on:
  push:
    branches: [main]
    paths:
      - 'isef/xmm_chandra_swift_local_null.py'
      - '.github/workflows/isef-xmm-chandra-swift-local-null.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: pip install astropy numpy scipy
      - run: python isef/xmm_chandra_swift_local_null.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: isef-xmm-chandra-swift-local-null
          path: results/xmm_chandra_swift_local_null.json
          if-no-files-found: error
