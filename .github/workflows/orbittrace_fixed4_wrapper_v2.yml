name: OrbitTrace fixed4 wrapper development v2

on:
  pull_request:
    branches:
      - agent/orbittrace-fixed4-blind-catalogue-synthesis
    paths:
      - orbittrace_fixed4_wrapper_v2/**
      - .github/workflows/orbittrace_fixed4_wrapper_v2.yml
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: orbittrace-fixed4-wrapper-development-v2
  cancel-in-progress: false

jobs:
  develop:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    env:
      SOURCE_SHA256: '918a7735c8f7a8ebecbd6e88514b3fa795eb23745f3de9992874304ab3d16388'
      SOURCE_BYTES: '36086'
      ENCODED_LENGTH: '13380'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/orbittrace-fixed4-wrapper-development-v2
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: ghoststream_fixed4_application/requirements.txt
      - name: Install frozen dependencies
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip -r ghoststream_fixed4_application/requirements.txt
          python -m pip install --no-deps gmn-python-api==0.0.13
      - name: Reconstruct and audit target-blinded source
        run: |
          set -euo pipefail
          mkdir -p output
          python - <<'PY'
          import ast, base64, gzip, hashlib, os
          from pathlib import Path

          parts = sorted(Path('orbittrace_fixed4_wrapper_v2/source_parts').glob('part*.b64'))
          expected = [f'part{i:02d}.b64' for i in range(4)]
          if [p.name for p in parts] != expected:
              raise RuntimeError(f'wrong source parts: {[p.name for p in parts]}')
          encoded = ''.join(''.join(p.read_text(encoding='ascii').split()) for p in parts)
          if len(encoded) != int(os.environ['ENCODED_LENGTH']):
              raise RuntimeError(f'encoded length mismatch: {len(encoded)}')
          source = gzip.decompress(base64.b64decode(encoded, validate=True))
          if len(source) != int(os.environ['SOURCE_BYTES']):
              raise RuntimeError(f'source bytes mismatch: {len(source)}')
          digest = hashlib.sha256(source).hexdigest()
          if digest != os.environ['SOURCE_SHA256']:
              raise RuntimeError(f'source SHA mismatch: {digest}')
          text = source.decode('utf-8')
          forbidden = [
              'F0059', '8958194010', '8957230278', '8814798136',
              '20220425100930_QG7RH', '247.17', '-14.34', '37.62',
              'PARTIAL_BLIND_ORBITTRACE_RECOVERY', 'GhostStream_Expert_Review_Bundle'
          ]
          found = [value for value in forbidden if value in text]
          if found:
              raise RuntimeError(f'OrbitTrace information present: {found}')
          required_literals = [
              'BLIND_LOW = 20.0', 'BLIND_HIGH = 55.0',
              'RANKING_VARIANTS', 'wrapper_blind_families.json.gz',
              'PASS_SUPPORT_NORMALIZED_WRAPPER_DEVELOPMENT',
              'FAIL_SUPPORT_NORMALIZED_WRAPPER_DEVELOPMENT'
          ]
          missing = [value for value in required_literals if value not in text]
          if missing:
              raise RuntimeError(f'missing frozen literals: {missing}')
          tree = ast.parse(text)
          names = {node.name for node in ast.walk(tree) if isinstance(node, (ast.FunctionDef, ast.ClassDef))}
          required_names = {
              'parse_catalogue', 'scan_year', 'component_records', 'build_families',
              'rank_families', 'label_profiles', 'exact_anchor_distances'
          }
          if not required_names.issubset(names):
              raise RuntimeError(f'missing interfaces: {sorted(required_names - names)}')
          Path('/tmp/run_fixed4_wrapper_v2.py').write_bytes(source)
          print('source bytes:', len(source))
          print('source SHA-256:', digest)
          PY
          python -m py_compile /tmp/run_fixed4_wrapper_v2.py
      - name: Run target-blinded wrapper development
        run: |
          set -euo pipefail
          python /tmp/run_fixed4_wrapper_v2.py \
            --candidate-payload sonotaco_fixed4_final_development/candidate.py.gz.b64 \
            --baseline-payload real_shower_meta_stage0/run_baseline_ceiling.py.gz.b64 \
            --scorer-parts mondrian_clique_development/source_parts_v2 \
            --output output
          cat output/ORBITTRACE_FIXED4_SUPPORT_WRAPPER_DEVELOPMENT.md
      - name: Enforce declared verdict
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          result = json.loads(Path('output/orbittrace_fixed4_support_wrapper_development.json').read_text())
          allowed = {
              'PASS_SUPPORT_NORMALIZED_WRAPPER_DEVELOPMENT',
              'FAIL_SUPPORT_NORMALIZED_WRAPPER_DEVELOPMENT',
          }
          if result.get('verdict') not in allowed:
              raise SystemExit(f"invalid verdict: {result.get('verdict')}")
          if not result.get('blind_family_payload_sha256'):
              raise SystemExit('missing pre-label family payload hash')
          print(json.dumps({
              'verdict': result['verdict'],
              'selected_variant': result['development']['selected_variant'],
              'blind_family_payload_sha256': result['blind_family_payload_sha256'],
          }, indent=2))
          PY
      - name: Record provenance
        if: always()
        run: |
          mkdir -p output
          sha256sum \
            orbittrace_fixed4_wrapper_v2/PROTOCOL.md \
            orbittrace_fixed4_wrapper_v2/source_parts/part00.b64 \
            orbittrace_fixed4_wrapper_v2/source_parts/part01.b64 \
            orbittrace_fixed4_wrapper_v2/source_parts/part02.b64 \
            orbittrace_fixed4_wrapper_v2/source_parts/part03.b64 \
            sonotaco_fixed4_final_development/candidate.py.gz.b64 \
            real_shower_meta_stage0/run_baseline_ceiling.py.gz.b64 \
            mondrian_clique_development/source_parts_v2/part00.b64 \
            mondrian_clique_development/source_parts_v2/part01.b64 \
            mondrian_clique_development/source_parts_v2/part02.b64 \
            mondrian_clique_development/source_parts_v2/part03.b64 \
            > output/source_sha256.txt 2>/dev/null || true
          python --version > output/python_version.txt
          python -m pip freeze > output/environment.txt
      - name: Upload wrapper-development evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: orbittrace-fixed4-wrapper-development-v2
          path: output
          if-no-files-found: error
          retention-days: 90
