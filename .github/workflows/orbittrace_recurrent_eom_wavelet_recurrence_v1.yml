name: OrbitTrace recurrent-EOM wavelet-recurrence GMN v1

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  disabled-during-exact-acceleration-repair:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "Scientific execution intentionally disabled while exact implementation-only acceleration is pinned and audited."
