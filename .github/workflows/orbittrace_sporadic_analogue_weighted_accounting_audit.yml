name: OrbitTrace sporadic-analogue weighted accounting audit

on:
  push:
    branches:
      - agent/orbittrace-sporadic-analogue-eom-v1
    paths:
      - 'orbittrace_sporadic_analogue_eom_v1/ACCOUNTING_AUDIT_ACTIVATE.md'

permissions:
  contents: read
  actions: read

env:
  PYTHONUNBUFFERED: '1'
  OMP_NUM_THREADS: '1'
  OPENBLAS_NUM_THREADS: '1'
  MKL_NUM_THREADS: '1'
  NUMEXPR_NUM_THREADS: '1'

jobs:
  accounting-audit:
    runs-on: ubuntu-22.04
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/orbittrace-sporadic-analogue-eom-v1
      - uses: actions/checkout@v4
        with:
          repository: brandonlign/runner
          ref: c9d6c44704013ba0c9430100e98a29a56b453304
          path: frozen-v8
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: frozen-v8/ghoststream_fixed4_application/requirements.txt
      - name: Install exact binding runtime and verify audit inputs
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip -r frozen-v8/ghoststream_fixed4_application/requirements.txt
          python -m pip install --no-deps gmn-python-api==0.0.13
          python -m pip install 'numpy==2.1.3' 'scipy==1.14.1' 'scikit-learn==1.7.1' 'hdbscan==0.8.43' 'charset-normalizer==3.5.0'
          mkdir -p input/v3 input/v8 input/ranker input/failed output
          git -C frozen-v8 fetch --no-tags --depth=1 origin d8258581af143308495bd97bedcc142abbbd951a
          git -C frozen-v8 show FETCH_HEAD:orbittrace_multi_anchor_energy_v3/multi_anchor_energy_v3.py > input/v3/multi_anchor_energy_v3.py
          test "$(git hash-object input/v3/multi_anchor_energy_v3.py)" = '2ba4835db23f8f623cdd28d0a4e6113b7954ecb2'
          cp frozen-v8/orbittrace_wavelet_catalogue_v3/wavelet_episode_comparator.py input/v3/wavelet_episode_comparator.py
          test "$(git hash-object orbittrace_sporadic_analogue_eom_v1/PROTOCOL.md)" = '0087c417e36b77c93daf869921219c7487f770ac'
          test "$(git hash-object orbittrace_sporadic_analogue_eom_v1/sporadic_analogue_eom.py)" = 'd5e4e3192b5566c545ec4c14a2728a01d974011c'
          test "$(git hash-object orbittrace_sporadic_analogue_eom_v1/run_development.py)" = '4fc3f4eb7c5c85951e49d813be4f598ef137667d'
          test "$(git hash-object orbittrace_sporadic_analogue_eom_v1/audit_weighted_accounting.py)" = '$(git hash-object orbittrace_sporadic_analogue_eom_v1/audit_weighted_accounting.py)'
          test "$(git hash-object orbittrace_recurrent_eom_hdbscan_v1/recurrent_eom.py)" = '30ac3fa3bc47910370df528fcf3ae8ecb6277b47'
          test "$(git hash-object orbittrace_recurrent_eom_hdbscan_v1/run_development.py)" = 'fdc4f3f6e037014aadfcc3ce41b7344aa0a80b2c'
          test "$(git hash-object orbittrace_density_synchronous_recurrent_eom_v1/density_synchronous_eom.py)" = '587a304f451e41b9503272f1783a6c6ebb295000'
          python -m py_compile orbittrace_sporadic_analogue_eom_v1/audit_weighted_accounting.py
      - uses: actions/download-artifact@v4
        with:
          name: orbittrace-sporadic-analogue-eom-v1-exact-runtime-retry
          path: input/failed
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
          run-id: 31912528972
      - name: Verify frozen failed-endpoint weights
        run: |
          set -euo pipefail
          test "$(sha256sum input/failed/SPORADIC_ANALOGUE_WEIGHTS.npy | cut -d' ' -f1)" = '648b88efc09192738dcce8eb2af15e215676dd62451a88cd9230337d80fd5347'
          test ! -e input/failed/SPORADIC_ANALOGUE_EOM_V1_PRELABEL.json
          test ! -e input/failed/SPORADIC_ANALOGUE_EOM_V1_GMN_DEVELOPMENT.json
      - uses: actions/download-artifact@v4
        with:
          name: orbittrace-active-urc-ranker-source-export-v1
          path: input/ranker
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
          run-id: 31344632499
      - uses: actions/download-artifact@v4
        with:
          name: orbittrace-pooled-year-centroid-v8-development
          path: input/v8
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
          run-id: 31217916558
      - name: Verify immutable GMN runtime inputs
        run: |
          set -euo pipefail
          test "$(sha256sum input/ranker/run_urc_union_ranker.py | cut -d' ' -f1)" = 'dd14e899ac08c4081cfee7d2dac2e54d2f25f78427cc4bee30f30296cd24b990'
          test "$(sha256sum input/v8/pooled_year_centroid_v8_development.json | cut -d' ' -f1)" = 'fa8f52cf046ced499a378cc6b7d04c52ef92bf0fa3f801049211d190f1c3919b'
      - name: Run pretruth weighted-accounting diagnostic
        env:
          PYTHONPATH: input/ranker:input/v3:orbittrace_sporadic_analogue_eom_v1:orbittrace_recurrent_eom_hdbscan_v1:orbittrace_density_synchronous_recurrent_eom_v1:frozen-v8/orbittrace_wavelet_catalogue_v3:frozen-v8:.
        run: |
          set -euo pipefail
          python -u orbittrace_sporadic_analogue_eom_v1/audit_weighted_accounting.py \
            --parent-runner orbittrace_recurrent_eom_hdbscan_v1/run_development.py \
            --quality-source input/ranker/run_urc_union_ranker.py \
            --support-source-parts frozen-v8/orbittrace_fixed4_support_wrapper_development/source_parts \
            --candidate-payload frozen-v8/sonotaco_fixed4_final_development/candidate.py.gz.b64 \
            --baseline-payload frozen-v8/real_shower_meta_stage0/run_baseline_ceiling.py.gz.b64 \
            --scorer-parts frozen-v8/mondrian_clique_development/source_parts_v2 \
            --v8-result-json input/v8/pooled_year_centroid_v8_development.json \
            --weights-npy input/failed/SPORADIC_ANALOGUE_WEIGHTS.npy \
            --output output
      - name: Enforce diagnostic firewall
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          r=json.load(open('output/WEIGHTED_ACCOUNTING_DIAGNOSTIC.json'))
          assert r['scientific_role']=='PRETRUTH_ENGINEERING_DIAGNOSTIC_ONLY'
          assert r['champion_candidate_count']==2094
          assert r['successor_score_computed'] is False
          assert r['successor_pretruth_created'] is False
          assert r['scientific_result_created'] is False
          assert r['hidden_truth_accessed'] is False
          for k in ['target_information_access','target_region_events_accessed','sonotaco_2013_2014_access','asfn_access','efn_access','amos_access','maarsy_scientific_access','dms_scientific_access']:
              assert r[k] is False, (k,r[k])
          print(r['verdict'], 'max_abs=',r['max_abs_sequential_residual'],'max_rel=',r['max_relative_sequential_residual'])
          PY
      - name: Preserve provenance
        if: always()
        run: |
          set +e
          git rev-parse HEAD > output/execution_commit.txt
          python --version > output/python_version.txt
          python -m pip freeze > output/environment.txt
          sha256sum orbittrace_sporadic_analogue_eom_v1/audit_weighted_accounting.py input/failed/SPORADIC_ANALOGUE_WEIGHTS.npy output/WEIGHTED_ACCOUNTING_DIAGNOSTIC.json > output/provenance_sha256.txt 2>/dev/null || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: orbittrace-sporadic-analogue-weighted-accounting-audit
          path: output
          if-no-files-found: error
          retention-days: 90
