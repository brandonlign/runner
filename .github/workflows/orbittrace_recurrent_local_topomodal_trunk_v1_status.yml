name: Probe OrbitTrace recurrent local TopoModal trunk v1 status

on:
  pull_request:
    branches: [agent/orbittrace-recurrent-local-topomodal-trunk-v1-activate]
    paths:
      - '.github/workflows/orbittrace_recurrent_local_topomodal_trunk_v1_status.yml'

permissions:
  contents: read
  actions: read
  issues: write
  pull-requests: write

env:
  TARGET_SHA: 7ba5d8f9f791f45f9804828acedfbff1101632e1
  TARGET_BRANCH: agent/orbittrace-recurrent-local-topomodal-trunk-v1-activate
  TARGET_WORKFLOW_NAME: OrbitTrace recurrent local TopoModal trunk v1 import repair

jobs:
  status:
    runs-on: ubuntu-22.04
    steps:
      - name: Read current binding status
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set -euo pipefail
          runs="$(gh api -X GET "repos/$REPO/actions/runs" -f branch="$TARGET_BRANCH" -f event=push -f per_page=100)"
          run_id="$(printf '%s' "$runs" | jq -r --arg sha "$TARGET_SHA" --arg name "$TARGET_WORKFLOW_NAME" '.workflow_runs[]? | select(.head_sha==$sha and .name==$name) | .id' | head -n1)"
          test -n "$run_id"
          meta="$(gh api "repos/$REPO/actions/runs/$run_id")"
          jobs="$(gh api "repos/$REPO/actions/runs/$run_id/jobs")"
          {
            echo '```text'
            echo "BINDING_RUN_ID=$run_id"
            printf '%s\n' "$meta" | jq -c '{status,conclusion,head_sha,run_attempt,created_at,updated_at,html_url}'
            printf '%s\n' "$jobs" | jq -c '.jobs[] | {id,name,status,conclusion,steps:[.steps[]|{name,status,conclusion}]}'
            echo '```'
          } > status.md
          gh pr comment "$PR_NUMBER" -R "$REPO" --body-file status.md
