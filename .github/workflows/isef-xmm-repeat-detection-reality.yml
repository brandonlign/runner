name: ISEF XMM repeat detection reality
on:
  push:
    branches: [main]
    paths:
      - 'isef/xmm_repeat_detection_reality.py'
      - '.github/workflows/isef-xmm-repeat-detection-reality.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  analyze:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: pip install astropy numpy scipy
      - run: python isef/xmm_repeat_detection_reality.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: isef-xmm-repeat-detection-reality
          path: results/xmm_repeat_detection_reality.json
          if-no-files-found: error
