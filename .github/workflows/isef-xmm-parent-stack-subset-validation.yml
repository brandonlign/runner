name: ISEF XMM parent stack subset validation
on:
  push:
    paths:
      - 'isef/xmm_parent_stack_subset_validation.py'
      - '.github/workflows/isef-xmm-parent-stack-subset-validation.yml'
  workflow_dispatch:
permissions:
  contents: read
  issues: write
jobs:
  validate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install 'numpy>=2.2' 'astropy>=7.1,<8'
      - run: timeout 600s python isef/xmm_parent_stack_subset_validation.py
      - uses: actions/upload-artifact@v4
        with:
          name: isef-xmm-parent-stack-subset-validation
          path: results/xmm_parent_stack_subset_validation.json
      - if: always()
        env: {GH_TOKEN: '${{ github.token }}'}
        run: |
          python - <<'PY' >/tmp/r.md
          import json
          from pathlib import Path
          print('## XMM parent-stack subset validation')
          p=Path('results/xmm_parent_stack_subset_validation.json')
          if p.exists(): print('```json\n'+json.dumps(json.loads(p.read_text()),indent=2,sort_keys=True)[:50000]+'\n```')
          PY
          gh issue comment 1447 --repo "$GITHUB_REPOSITORY" --body-file /tmp/r.md
