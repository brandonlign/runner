# Frozen fresh external-validation retry trigger

Execution-only marker after a pre-data GitHub Actions artifact-name correction. It changes no protocol, scientific code, parser, survey year, archive URL/member, proposal rule, ranking rule, scaled endpoint, pass gate, blindness boundary, or OrbitTrace access rule.
