# SACV recurrence-pair v2 continuation — frozen before GMN outcome

This continuation is fixed while the first target-excluded GMN v2 run is queued/running.

If and only if `PASS_M2D_SACV_PAIR_V2_GMN_DEVELOPMENT` occurs, transfer the exact v2 rule once, without retuning, to the already-established exposed SonotaCo SACV benchmark. The same local hypothesis construction, physical scales, support, contamination, seasonal analogs, reciprocal validation, pair score ordering, membership union, and fallback are immutable. All SonotaCo v2 memberships must freeze before its truth is opened. The transfer must preserve the existing SACV-v1 paired quality contract; failure stops continuation.

If and only if both GMN and SonotaCo pass, execute one target application over original M2D ranks 1..100. Stage A must not receive the canonical OrbitTrace package and freezes every v2 extraction first. Stage B then performs exact trajectory-ID intersection only.

Target success is unchanged: original M2D rank <=100; >=4 exact canonical IDs in 2022; >=4 in 2023; >=8 total; exact target F1 >0.5. Report extracted membership size, exact overlap, precision, recall, F1, annual support, and original rank.

No target-aware pair choice, reranking, threshold/radius/support change, component union, halo growth, merging, fallback exception, or post-target rescue is allowed. Because this exact v2 architecture was developed after the #1406/#1407 target reveal, a target success is a no-retuning post-reveal extraction confirmation, not a retroactive pristine blind rediscovery claim.
