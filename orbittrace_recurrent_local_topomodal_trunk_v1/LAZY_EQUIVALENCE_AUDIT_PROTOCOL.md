# Recurrent local TopoModal trunk v1 — lazy transport equivalence audit

## Status

**FROZEN ENGINEERING AUDIT BEFORE EXECUTION. NO SHOWER TRUTH.**

The frozen scientific method remains `PROTOCOL.md` (`de8d040a1f9d3b0825ce56532efd5950acefc689`) and `build_prelabel.py` (`cd3fb15263fd4b2e38e4b413ece9b347b64816d5`). This audit does not define a successor or change any scientific rule. It tests whether `technical_lazy_local_trunk.py` is an exact transport/storage implementation of the frozen `local_trunk()` operation.

## Prior execution status

Binding attempt `32091272769` is an engineering no-result. All frozen source/input audits and label-free geometry export completed. The frozen prelabel build began with rank 1 (`n=32458`) and then the hosted runner received an external shutdown/cancel signal. No prelabel artifact, shower-truth evaluation, scientific metric, or verdict was produced.

## Candidate population

Use the exact binding density-synchronous parent prelabel:

- run `31852836840`;
- artifact `9238142199`;
- prelabel SHA-256 `efce0617a738a0372ec5b007fb87b46912accd8419f0f768829c4c0cb7d62993`;
- exactly 2,094 ordered parents.

Use the exact label-free target-excluded GMN geometry reconstructed by frozen `export_geometry.py`; protected `[20,55]` is excluded and shower truth is not exported.

## Exact equivalence sample

Compare the frozen original `local_trunk()` and `local_trunk_lazy()` on the union of:

1. **every parent with member count <=256**; and
2. exact fixed stress ranks `{1,2,4,6,10,13}`.

The size rule and stress ranks are frozen before audit execution. In the binding parent prelabel the <=256 rule covers 2,027/2,094 slots; the six stress ranks deliberately exercise the largest/densest early-catalogue parents, including the two largest parents. No shower labels or outcome-dependent selection enters this sample.

## Required exact equality

For every audited parent require byte/semantic equality of:

- final event-ID list;
- complete returned topology-summary dictionary, including radius-degree summaries, leaf/internal/root counts, anchor event/density, anchor-chain node memberships and hashes, annual support counts, decision token, removed count, and final membership hash.

No tolerance is permitted for membership or serialized summary fields. Numeric values must compare with Python exact equality because both implementations use identical coordinates, exact radius, exact degree counts, and the same GUDHI runtime.

Also require the lazy implementation to report exactly one requested neighbor row per input event.

## Interpretation

Return `PASS_LOCAL_TRUNK_LAZY_EQUIVALENCE_AUDIT` only if every audited parent is exactly equal. Any mismatch is a technical FAIL and blocks lazy execution of the frozen science.

A PASS authorizes exactly one technical repair execution of the already-frozen full 2,094-parent prelabel using `technical_build_prelabel_validated_lazy.py`, followed by the unchanged frozen truth evaluator only after the complete prelabel is sealed. It does not authorize changes to radius, support floor, anchor, trunk depth, parent order, truth metrics, gates, target access, external data, or any other science.
