# SonotaCo 2023 fixed-4° replacement-replication source audit

Status: frozen after the header-only pass in PR #142 and before any SonotaCo 2023 meteor row, label, support count, episode, score, p-value, fold, or scientific endpoint is read or computed.

## Frozen model and panel

The detector is the exact fixed-4° coverage-normalized Mondrian anchored four-clique method frozen by the standalone SonotaCo 2025 final-development pass in PR #136. No model, scale, score, calibration count, seed family, alpha, comparator, fold rule, or scientific gate may change.

The independent panel is the deterministic SonotaCo 2023 replacement selected without scientific content in PR #141 and found structurally compatible by the one-line audit in PR #142:

- archive: `https://www.astro.sk/iaumdcDB/public/data/SNMv3/023a.zip`;
- archive SHA-256: `9f44696f99164801ff405dab90f68df3666b0d6734fed464a95e7ed0d6f5f430`;
- member: `023a/_U2_20230101_S.csv`.

## Source-only audit contract

This PR may only decode, hash, compile, and statically inspect the candidate. It must prove:

1. candidate source SHA-256 `3030eb185d8d8fa6597eff20ada9a0194d4de00adc81077bb031b0c25400bff9`;
2. the fixed-4° pairwise distance and anchored quartet selection functions are AST-identical to the final-model source SHA-256 `747b2b1471f3ba193d68a39dd82ad3ac8506be63b651d45f84ffabb8d1acd301`;
3. year, corpus, archive, and member constants are exactly the frozen 2023 values;
4. the 20°–55° inclusive blind exclusion occurs before native-label access;
5. the exact inherited calibration, negative, support, and positive seed prefixes are retained;
6. calibration/test counts and the complete FPR, AUROC, fold, k=4/k=6/k=8, and monotonicity gates are present unchanged;
7. no scale sweep, selector, fusion, drift fit, reserved-panel resource, or GhostStream value appears.

A pass authorizes only one separate one-shot SonotaCo 2023 replacement independent replication. The later result must be reported as replacement replication after a reserved-panel sequencing breach, not as the originally planned confirmation.
