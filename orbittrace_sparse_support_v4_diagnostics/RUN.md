# Diagnostic execution trigger

This child branch exists only to trigger the already-frozen artifact-only sparse-support v4 diagnostic workflow through a pull request into `agent/orbittrace-sparse-support-v4-diagnostics`.

It changes no scientific code, input, threshold, ranking rule, catalogue boundary, or target-blinding rule.
