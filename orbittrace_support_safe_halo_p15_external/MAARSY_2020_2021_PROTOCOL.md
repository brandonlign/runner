# P15 no-retuning external validation — MAARSY 2020/2021

## Status / eligibility

Protocol-only reservation frozen before any P15 matched Sugar/HDBSCAN truth, superiority result, or P15 development compatibility result is known. It may execute only if P15 first passes target-excluded development compatibility and the matched benchmark returns exact verdict `PASS_P15_MATCHED_SPARSE_SUPERIORITY_BOTH_COMPARATORS_BOTH_YEARS`, requiring sparse superiority separately against catalogue HDBSCAN and Sugar in both SonotaCo 2023 and 2025. Broad-only superiority is insufficient.

No MAARSY scientific value is opened by this reservation. Solar longitude 20°–55° and all OrbitTrace target information remain inaccessible.

## Fixed external panel and transport

External panel is exactly **MAARSY 2020 and 2021**. Years may not be replaced, subsetted, augmented, or selected after performance is known.

Survey transport remains the already-source-audited MAARSY adaptation frozen before matched truth:
- exact Zenodo record/file identity and archive size/MD5 remain fixed;
- exact native geometry fields `sun_lon`, `slat`, `slon`, `vels` and native six-column `kepler` orbit representation are used;
- solar longitude 20°–55° is excluded before radiant/speed values are retained;
- deterministic fixed 10°-bin identity cap remains 10,000 events per bin;
- years are exactly 2020/2021, stopping before the first 2022 member;
- no schema/value-dependent feature substitution or threshold adjustment is allowed.

If an exact frozen scientific observable cannot be represented from the MAARSY schema, verdict is `ARCHITECTURE_INCOMPATIBLE` rather than substituting another quantity or retuning the method.

## Frozen P15 method

P15 remains dual-output.

### Primary discovery core

Primary discovery is exactly promoted P14 recurrent-core construction plus support-safe multiplicity total-order semantics:
- `EPISODE_SIZE=128` is immutable;
- every multiplicity-scorable recurrent core uses exact frozen v8 scoring/ranking;
- if a recurrent core lacks required 128-event local support, it keeps its exact core identity, receives no fabricated score, and ranks after all scorable cores in stable lexical `family_id` order;
- any other primary scoring exception is fatal.

### Secondary characterization halo

P15 changes only exact-P12 halo **availability**:
- all P12 drift, observation/orbit features, cross-fit, finite-sample rank, reciprocal reliability, geometry, density veto, responsibility, conflict resolution, and membership rules are exact and unchanged for an eligible family-direction;
- `MIN_DIRECTION_NEGATIVES=128` is immutable;
- a family-direction with fewer than 128 exact target-window nonseed negatives is deterministically recorded `CHARACTERIZATION_UNAVAILABLE_INSUFFICIENT_NEGATIVES`, adds zero nonseed proposals, and is skipped;
- no padding, resampling, replacement, reduced negative set, threshold extrapolation, score fabrication, or borrowing across years/panels is allowed;
- the reciprocal direction is independently eligible;
- every other exception remains fatal;
- halo membership can never create, delete, reorder, rescue, or qualify a primary core discovery.

P15 is eligible for external execution only after its development gate establishes that this availability fallback is completely vacuous on the original target-excluded development panel and that canonical P12/P13 outputs remain exact.

## Pretruth firewall

Before any MAARSY known-shower label value is indexed, freeze/hash:
- exact target-excluded 2020/2021 event universes;
- archive/source/schema/unit identities and transformations;
- recurrent core families/event IDs;
- complete primary core order and exact scored/unscorable support audit;
- every P15 halo-availability decision, including family ID, source/target year, observed negative count, fixed required count 128, status, and canonical availability-ledger hash;
- exact halo memberships produced only by eligible directions;
- all model/cross-fit/density/drift/decision payloads used by eligible directions;
- proof no 20°–55° event entered candidates, backgrounds, families, episodes, folds, scores, availability ledgers, endpoints, checkpoints, or truth universe.

No external truth may feed back into family generation, membership, support eligibility, ranking, thresholds, or availability.

## Power classification

Panel is power-eligible only if, after frozen target exclusion and before performance interpretation:
- at least **5 known-shower labels have >=4 usable events in each of 2020 and 2021**; and
- P15 produces at least **5 recurrent primary core families**.

Otherwise verdict is `POWER_INCONCLUSIVE_P15_MAARSY_2020_2021`, not pass/fail, with no target authorization and no MAARSY-specific retuning.

## No-retuning generalization gates

For a power-eligible panel:

### Core discovery integrity
- exact P15/P14 primary core construction and rank complete without integrity/firewall violation;
- immutable core seeds are preserved;
- at least one externally truth-qualified recurrent shower is recovered by the core;
- no halo event or availability decision affects core qualification/recovery/rank;
- support-safe multiplicity obeys exact 128-event semantics with no fabricated score and every unscorable core after every scored core.

### Halo characterization generalization

Evaluate core and final P15 halo on the **same frozen eligible external truth set**. Do not remove a shower from evaluation because one or both halo directions were unavailable.

- halo macro-F1 >= core macro-F1 + **0.08**;
- for externally large showers (same frozen development definition, total usable truth members >=100 when nonempty), halo mean recall >= **1.5×** core mean recall;
- when that large subset is nonempty, halo mean precision >= **0.85**;
- if the large subset is empty, the two large-shower gates are N/A rather than manufactured PASS; core-discovery and macro-F1 gates remain mandatory.

All P15 unavailable-direction counts and identities are descriptive pretruth diagnostics only; they cannot be used to subset truth or alter gates.

Final power-eligible PASS is `PASS_P15_MAARSY_2020_2021_NO_RETUNING_EXTERNAL`; otherwise `FAIL_P15_MAARSY_2020_2021_NO_RETUNING_EXTERNAL`.

## Downstream authorization

Only exact P15 matched PASS **and** exact P15 MAARSY external PASS authorize activation of the separately frozen final blind target-free OrbitTrace search. Matched FAIL, external FAIL, power-inconclusive, architecture-incompatible, or any integrity/firewall failure leaves target access forbidden.
