# P18 P17-rank / label-free-halo-membership catalogue — primary matched challenger freeze

## Status

Frozen before any successful P17 matched pretruth checkpoint exists, before any P17/P18 comparator cluster value or known-shower matched truth value is opened, before external evaluation, and without OrbitTrace target or 20°–55° target-region access.

P18 is the **sole primary literature challenger** for the next matched Sugar/HDBSCAN evaluation. P17 core-only membership is retained only as a preregistered diagnostic ablation. The choice between core-only and halo membership may not be changed after matched truth is opened.

## Scientific rationale fixed pretruth

Two already-established target-excluded facts motivate the composition:

1. P14/P15/P17 recurrent cores provide the discovery-family universe and support-safe multiplicity rank. P17 changes only fail-closed reciprocal bookkeeping for a P15 support-unavailable secondary direction; it changes no discovery score, family construction, threshold, rank, or positive proposal rule.
2. The separately frozen P16 development result established that, for the same 226 development families, the exact label-free P12/P15 halo is a one-to-one superset of each core and materially improves membership quality without changing family existence or rank: canonical halo macro F1 `0.37661279333940806` versus core/v8 macro F1 `0.173666...`; 17,238 added IDs are already frozen label-free decisions, not a new P18 proposal.

P18 therefore composes the strongest already-frozen roles rather than selecting a role after seeing matched performance.

## Exact architecture

For each comparator-specific **successful P17 pretruth checkpoint**, before any comparator cluster value or known-shower truth is opened:

- family existence is exactly the P17/P14 recurrent-core family universe;
- primary rank is exactly the P14 support-safe multiplicity total order carried unchanged through P15 and P17;
- P17's fail-closed reciprocal-closure ledger and all P15 unavailable-direction provenance remain unchanged and frozen;
- evaluator-facing/reportable membership is exactly the already-frozen P17/P15/P12 halo family with the same stable family ID;
- every core event set must be a subset of its halo event set;
- stored halo-added IDs must equal exactly `halo IDs − core IDs`;
- the transformation may add only evaluator-compatibility `p3_added_event_ids = halo − core` plus hashes/provenance;
- no detector, score, distance, threshold, family, member proposal, growth step, merge, or rank may be recomputed;
- halo-added members cannot seed any additional growth;
- P17 support-unavailable directions remain unavailable; P18 cannot fill, pad, resample, borrow, lower 128, or manufacture missing halo evidence.

The transformation is the P16 output-semantic operation applied to a P17 checkpoint, with the additional invariant that every P17 closure field survives unchanged.

## Fixed matched benchmark and promotion standard

P18 inherits unchanged the frozen SonotaCo 2023/2025 matched row universes, HDBSCAN and Sugar assignment inputs, common-truth denominator, matching algorithm, F1 calculations, false-positive-burden calculations, and pairwise sparse-superiority gates.

Promotion requires `SPARSE_STREAM_SUPERIORITY` separately against **both catalogue HDBSCAN and Sugar in both 2023 and 2025** under the already-frozen gates:

- 4–9-member mean F1 gain >= +0.10;
- combined 4–24-member mean F1 gain >= +0.10;
- overall macro-F1 delta no lower than -0.10;
- retain at least 80% of the comparator count of showers with F1 > 0.5;
- all common-universe, provenance, integrity, and firewall gates pass.

Broad superiority is supplementary and cannot substitute for sparse superiority. Failure against either comparator or either year is a matched no-go for P18 and does not authorize external validation.

## Pretruth candidate-selection rule

P18 is selected now as the primary challenger because it combines the already-frozen discovery/ranking role with the already-frozen label-free membership role. This selection is not conditional on P17 core-only matched metrics.

After truth is opened:

- P18 determines the promotion verdict;
- P17 core-only matched metrics may be reported only as an ablation explaining the value/cost of halo membership;
- no rule may switch the promoted candidate back to P17 core-only because P18 underperformed on some observed matched endpoint;
- no mixture, per-family switch, per-size-bin switch, or comparator-specific switch between core and halo membership is permitted.

## Required immutable prerequisites

Before P18 checkpoint transformation can execute:

- P17 development-vacuity PASS run `31332157812`, artifact `9043232586`, ZIP digest `sha256:f89e1330da616e4224bec063e82194042f6e5625cf02cf851f8d6e511d408852`;
- exact P17 matched scientific source SHA256 `c0c39d1bd660efbe5e5353b5a33185428a6f60f4a3759be3acd16a15a063012a`;
- P16 canonical output-architecture PASS run `31331471689`, artifact `9043046136`, ZIP digest `sha256:fc87c995b489d791b8df279002062b9df601140889f408d4ec3ec3222b91fdc8`;
- exact P16 transformation semantics represented by adapter blob `a143d59be30a03091ae9a46ce169ff9b12280c99`;
- a successful P17 pretruth artifact containing both HDBSCAN and Sugar checkpoints, with comparator cluster values and known-shower truth still unread.

## External-validation and target boundary

Only a P18 matched sparse-superiority PASS may advance to independent no-retuning external validation. The external panel remains the already-preregistered MAARSY 2020/2021 route; P18 must receive a source-only external transport/addendum before any MAARSY result is opened, preserving the same family-existence/rank versus halo-membership separation and all existing power/effect gates.

OrbitTrace target access remains forbidden. Even after external PASS, final discovery must use the already-frozen Stage-A target-free primary family ranking and exact-ID Stage-B reveal firewall. Halo membership may characterize a discovered family but may not create, qualify, rescue, merge, or rerank the family for final blind recovery.
