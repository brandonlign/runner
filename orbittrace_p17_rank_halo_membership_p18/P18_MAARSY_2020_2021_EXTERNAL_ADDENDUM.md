# P18 no-retuning external-validation addendum — MAARSY 2020/2021

## Status

Source/protocol-only addendum frozen before any P18 matched Sugar/HDBSCAN result, before any P18 MAARSY event-value access, and without OrbitTrace target or 20°–55° target-region access.

This addendum is dormant unless the exact frozen P18 primary challenger passes the already-frozen matched `SPARSE_STREAM_SUPERIORITY` standard against both catalogue HDBSCAN and Sugar in both SonotaCo 2023 and 2025. No weaker, broad-only, or diagnostic P17 core-only result can activate external validation.

## External panel and transport inherited unchanged

P18 inherits the already-preregistered MAARSY design without changing panel, transport, sampling, power, or effect-size gates:

- panel exactly MAARSY 2020 and 2021;
- exact preaccess transport lineage remains original blob `f563ca63e169e6ddfe846219d8f00384f3451797`, adapter blob `bbf71f190ca128e3e24f71900fefbd1472cdacb6`, source-audit run `31325401462`, generated transport SHA256 `3e0bcc3db805f60310ec9b01d6d48f76b29544780119c05e71fc225c04e3fe48`;
- fixed 10°-bin identity cap remains 10,000 events per bin;
- solar longitude 20°–55° remains excluded before scientific values are retained;
- power eligibility remains at least 5 truth labels with >=4 usable events in each year and at least 5 recurrent cores;
- a power-inconclusive panel remains distinct from scientific failure;
- for a power-eligible panel, halo macro-F1 must exceed core macro-F1 by >=0.08;
- when the externally large-shower subset is nonempty, halo recall must be >=1.5x core recall and halo precision must be >=0.85;
- every inherited core-discovery, integrity, provenance, and firewall gate remains mandatory;
- no external result may alter family generation, ranking, thresholds, membership parameters, years, transport, truth eligibility, power gates, effect gates, or target authorization.

No MAARSY-specific tuning, panel/year substitution, threshold relaxation, alternate observable, result-driven rescue, or comparator-aware adaptation is permitted.

## Exact P18 architecture on MAARSY

P18 preserves the same precommitted separation used for the matched benchmark:

1. **Family existence and rank** come only from the exact frozen P17/P14 recurrent core and support-safe multiplicity order.
2. **Reported membership** is the exact frozen label-free P17/P15/P12 halo for each already-discovered family.
3. Halo membership cannot create, delete, qualify, rescue, merge, or reorder a family, and halo-added members cannot seed additional growth.
4. `MIN_DIRECTION_NEGATIVES=128` remains immutable.
5. A secondary characterization direction with `<128` target-window negatives remains `CHARACTERIZATION_UNAVAILABLE_INSUFFICIENT_NEGATIVES` and contributes zero proposals; no padding, resampling, borrowing, or threshold lowering is allowed.
6. P17 reciprocal closure remains fail-closed: if a reciprocal direction is absent only because the frozen P15 availability ledger proves that reciprocal support is `<128`, reciprocal reliability is false and the supported direction cannot contribute a P9-bidirectional proposal. A missing reciprocal for any other reason is fatal.
7. The P17 reciprocal-closure ledger and the P18 core↔halo correspondence/membership hashes must freeze before any MAARSY truth value is indexed.
8. P18 cannot switch to P17 core-only membership, per-family core/halo selection, per-size-bin selection, or any comparator/external-result-dependent membership mixture.

P18 therefore changes no external discovery mechanism relative to the promoted core architecture; it fixes the output semantics in advance: discovery/rank from the recurrent core, membership from the already-frozen label-free halo.

## Immutable prerequisites

External execution is authorized only if all of the following are independently verified from immutable artifacts:

- P17 artifact-only development-vacuity PASS run `31332157812`, artifact `9043232586`;
- exact P17 matched scientific source SHA256 `c0c39d1bd660efbe5e5353b5a33185428a6f60f4a3759be3acd16a15a063012a`;
- P16 canonical output-architecture PASS run `31331471689`, artifact `9043046136`, establishing the pretruth core-rank/halo-membership operation on target-excluded development;
- exact P18 transform blob `3f53e1ffeabb1b32d2de124cbf903d193012b9af`;
- exact frozen P18 matched checkpoints were created before comparator cluster values/known-shower truth;
- exact P18 matched verdict is `PASS_P18_MATCHED_SPARSE_SUPERIORITY_BOTH_COMPARATORS_BOTH_YEARS`;
- the P18 matched result explicitly records P17 core-only as diagnostic ablation only and `p18_no_posttruth_core_halo_switch=true`.

The SonotaCo matched panel cannot count as independent generalization. Previously exposed GMN panels cannot authorize target access.

## External verdict and target boundary

For a power-eligible panel, the passing verdict is `PASS_P18_MAARSY_2020_2021_NO_RETUNING_EXTERNAL`; otherwise the scientific failure verdict is `FAIL_P18_MAARSY_2020_2021_NO_RETUNING_EXTERNAL`. An underpowered panel remains `POWER_INCONCLUSIVE_MAARSY_2020_2021`. An exact frozen operation that cannot be represented by the MAARSY schema remains `ARCHITECTURE_INCOMPATIBLE` and cannot be repaired after seeing event values.

Only exact P18 matched sparse-superiority PASS plus exact P18 MAARSY no-retuning external PASS may satisfy the methodology/literature/generalization prerequisites for the separately frozen final target-free blind-search firewall.

This addendum authorizes no OrbitTrace target access and must not be changed in response to any matched, external, or final blind-search result.
