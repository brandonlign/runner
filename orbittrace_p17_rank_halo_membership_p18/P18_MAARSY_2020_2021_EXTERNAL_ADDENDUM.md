# P18 no-retuning external-validation addendum — MAARSY 2020/2021

## Status

Source/protocol-only addendum frozen before any successful P17/P18 matched checkpoint result, before any matched comparator cluster value or known-shower truth value is opened, before any MAARSY event value is accessed, and without OrbitTrace target or target-region event access.

This addendum cannot execute external data. It applies only if the exact frozen P18 primary challenger passes the already-frozen sparse-superiority requirement separately against catalogue HDBSCAN and Sugar in both SonotaCo 2023 and 2025. Broad-only superiority does not authorize this stage.

## Inherited external panel and transport — unchanged

P18 inherits the already-preregistered MAARSY external route without selecting a new survey, year pair, power floor, effect threshold, or transport after matched truth:

- years remain exactly MAARSY 2020 and 2021;
- exact preaccess transport remains original blob `f563ca63e169e6ddfe846219d8f00384f3451797`, adapter blob `bbf71f190ca128e3e24f71900fefbd1472cdacb6`, source-audit run `31325401462`, generated transport SHA256 `3e0bcc3db805f60310ec9b01d6d48f76b29544780119c05e71fc225c04e3fe48`;
- fixed 10°-bin identity cap remains 10,000 events per bin;
- solar longitude 20°–55° remains excluded before scientific values are retained;
- the immutable recurrent-core family construction and support-safe multiplicity rank remain the P14/P17 primary discovery output with `EPISODE_SIZE=128`, no fabricated score, and unscorable cores ordered after every scored core;
- the secondary halo keeps exact `MIN_DIRECTION_NEGATIVES=128`; a direction with fewer than 128 eligible negatives is recorded unavailable and contributes zero nonseed proposals, with no padding, borrowing, resampling, threshold relaxation, or observable substitution;
- P17 reciprocal closure applies exactly: when and only when the frozen unavailability ledger proves the reciprocal direction is `CHARACTERIZATION_UNAVAILABLE_INSUFFICIENT_NEGATIVES`, the missing reciprocal reliability is represented as false, so the inherited bidirectional AND rule remains fail-closed and the supported direction contributes no proposals; any other missing reciprocal remains fatal;
- power eligibility remains at least 5 truth labels with at least 4 usable events in each year and at least 5 recurrent cores;
- `POWER_INCONCLUSIVE_MAARSY_2020_2021` remains distinct from scientific failure;
- for a power-eligible panel, exact label-free halo macro-F1 must exceed immutable core macro-F1 by at least 0.08;
- when the externally large-shower subset is nonempty, exact halo recall must be at least 1.5 times immutable core recall and exact halo precision must be at least 0.85;
- every inherited discovery, integrity, information-parity, and firewall gate remains mandatory.

No P18 result may change the survey, years, transport, power floor, truth eligibility, family generation, rank, halo proposal rule, reciprocal-closure rule, thresholds, or target authorization.

## Sole P18 output architecture

P18 adds no detector computation. For every externally discovered recurrent family:

1. family existence and qualification come only from the immutable P17 recurrent core;
2. family rank comes only from the immutable P14/P17 support-safe multiplicity order;
3. the evaluator-facing/reportable membership is exactly the already-frozen P17/P15/P12 label-free halo with the same stable family ID;
4. core IDs must be a subset of halo IDs and stored halo additions must equal exactly `halo IDs − core IDs`;
5. P18 may not propose a new event, recompute a distance/score, create/delete/merge a family, alter qualification, rerank a family, or allow halo-added events to seed growth;
6. all P17 closure and P15 unavailability provenance must survive byte/semantic validation into the external checkpoint;
7. core↔halo correspondence, primary-order hash, closure-ledger hash, and reported-membership hash must freeze before any MAARSY truth value is indexed.

The external effect-size gates therefore compare the immutable core reference membership with P18's exact frozen halo membership, exactly as preregistered before matched truth.

## Immutable prerequisites

External execution is authorized only if immutable artifacts establish all of the following:

- P17 development-vacuity PASS run `31332157812`, artifact `9043232586`, ZIP digest `sha256:f89e1330da616e4224bec063e82194042f6e5625cf02cf851f8d6e511d408852`;
- exact P17 matched scientific source SHA256 `c0c39d1bd660efbe5e5353b5a33185428a6f60f4a3759be3acd16a15a063012a`;
- P16 canonical output-architecture PASS run `31331471689`, artifact `9043046136`, ZIP digest `sha256:fc87c995b489d791b8df279002062b9df601140889f408d4ec3ec3222b91fdc8`;
- P18 checkpoint transformation is the already-frozen P16 halo-membership operation applied to a successful P17 checkpoint while preserving every P17 closure field and the exact primary order;
- both HDBSCAN and Sugar P18 checkpoints were frozen before matched truth;
- the precommitted P18 evaluator establishes `PASS_P18_MATCHED_SPARSE_SUPERIORITY_BOTH_COMPARATORS_BOTH_YEARS` under the unchanged pairwise gates.

Any sparse failure against either comparator in either year, any underpowered/incomplete matched panel, or broad-only superiority does not authorize external validation.

## External verdict and target boundary

For a power-eligible panel, the only passing verdict is `PASS_P18_MAARSY_2020_2021_NO_RETUNING_EXTERNAL`; otherwise the scientific verdict is `FAIL_P18_MAARSY_2020_2021_NO_RETUNING_EXTERNAL`. Underpowered remains `POWER_INCONCLUSIVE_MAARSY_2020_2021`. An exact frozen scientific observable/operation that cannot be represented from the preregistered MAARSY transport remains `ARCHITECTURE_INCOMPATIBLE` and may not be repaired by retuning to MAARSY outcomes.

Only exact P18 matched sparse-superiority PASS plus exact `PASS_P18_MAARSY_2020_2021_NO_RETUNING_EXTERNAL` can satisfy the methodology/generalization prerequisites for the separately frozen exact-ID blind search. This addendum authorizes no OrbitTrace target access and must not be edited in response to matched, MAARSY, or target outcomes.
