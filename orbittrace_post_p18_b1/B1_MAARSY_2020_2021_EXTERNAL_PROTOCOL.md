# B1 no-retuning external validation — MAARSY 2020/2021

## Status

Protocol-only reservation frozen while the exact pre-P18 B1 development run is still in progress, before any B1 development result is known, before any SonotaCo 2014/2016 fresh matched result, and before any MAARSY event value is accessed.

This protocol is dormant unless:

1. exact frozen B1 passes its original target-excluded GMN 2022/2023 development gates; and
2. the unchanged B1 architecture subsequently passes the reserved fresh SonotaCo 2014/2016 sparse-superiority benchmark separately against both catalogue HDBSCAN and Sugar in both years.

No weaker result activates MAARSY.

## Frozen external panel

The independent external panel remains exactly **MAARSY 2020 and 2021** under the already-reserved preaccess transport lineage:

- original transport blob `f563ca63e169e6ddfe846219d8f00384f3451797`;
- schema adapter blob `bbf71f190ca128e3e24f71900fefbd1472cdacb6`;
- source audit `31325401462`;
- generated transport SHA256 `3e0bcc3db805f60310ec9b01d6d48f76b29544780119c05e71fc225c04e3fe48`;
- fixed 10°-bin identity cap 10,000 events per bin;
- solar longitude 20°–55° excluded before any scientific value or truth label is retained.

The SonotaCo matched panel cannot count as external generalization.

## Exact B1 architecture — no retuning

External B1 must preserve the original pre-P18 frozen rule byte/semantically:

- primary family universe, original seeds, pooled centroids, multiplicity score/rank remain the frozen v8 discovery core;
- for target year `y`, train membership models only from original other-year family seeds;
- features remain exactly `log1p(second-nearest v8 geometry distance, order-1 solar-longitude trajectory residual, median Southworth-Hawkins D_SH to original source seeds)`;
- stream density remains a 3-D Ledoit-Wolf Gaussian;
- local-field density remains a 3-D Ledoit-Wolf Gaussian on same-source-year screened non-family events, retaining other recurrent families as competitors;
- prior odds remain `N original orbit-valid source seeds / N screened source background`;
- inherited candidate support bounds remain activity padding ±6°, density `d2 <= 1.5`, trajectory residual `<=1.5`;
- membership cutoff remains transferred log posterior odds strictly `>0`;
- original seeds retain absolute priority; added events are nonrecursive and cannot alter family topology, centroids, scores, rank, or training support;
- conflict resolution remains larger log odds, then smaller orbital median, then smaller `d2`, then stable family ID;
- no feature, weight, prior multiplier, covariance model, posterior cutoff, activity padding, geometry/orbit threshold, trajectory order, family construction, recursion, rank, or rescue search is permitted.

The exact B1 development source remains SHA256 `8b848cad728ad2370d1bf538b3ccc7f71d1b65c1f8d483838374aa6e12bbbb0d`.

## Schema compatibility is fail-closed

B1 requires per-event information sufficient to reproduce its exact three features, including an exact orbit representation from which the frozen Southworth-Hawkins `D_SH` can be computed without changing the statistic.

Before first MAARSY event-value access, a source-only compatibility audit must prove the frozen MAARSY transport exposes all required observables with deterministic unit/convention mapping. If exact `q/e/i/peri/node`-equivalent orbit information cannot be reconstructed without an outcome-dependent model, proxy, imputation, or changed distance statistic, the verdict is `ARCHITECTURE_INCOMPATIBLE_B1_MAARSY_2020_2021`. No alternate external dataset may be chosen after viewing MAARSY scientific values under this protocol.

## Frozen power and scientific gates

The inherited external power floor remains:

- at least 5 eligible truth shower labels;
- each eligible label has at least 4 usable events in each year;
- at least 5 recurrent B1/v8 primary families.

If this is not met, verdict is `POWER_INCONCLUSIVE_B1_MAARSY_2020_2021`, not scientific failure.

For a power-eligible panel, compare B1's frozen final membership against immutable original-core membership on the same discovered family universe. Passing requires:

- B1 macro-F1 >= core macro-F1 +0.08;
- if the externally defined large-shower subset is nonempty, B1 recall >=1.5x core recall and B1 precision >=0.85;
- every inherited family-universe, input-identity, information-parity, integrity, target-exclusion, and no-retuning gate passes.

The only scientific PASS is `PASS_B1_MAARSY_2020_2021_NO_RETUNING_EXTERNAL`. Otherwise a power-eligible scientific result is `FAIL_B1_MAARSY_2020_2021_NO_RETUNING_EXTERNAL`.

## Target boundary

Even a B1 external PASS authorizes only the separately frozen final Stage-A source implementation and prerequisite audit. It does not itself expose the withheld OrbitTrace reference.

The OrbitTrace target, target reference, and 20°–55° target-region discovery data remain inaccessible throughout B1 development, fresh matched benchmarking, and this external validation.
