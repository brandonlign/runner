# OrbitTrace Persistable persistence-ladder cross-scale result v1

## 🔴 NEGATIVE

Authoritative run: `31955028737`

Artifact: `9265748389`

Artifact digest: `sha256:b5612bb47298e037e1fe5cac131b8f6135e3dc4bb1447ea062cd2ce329a40db1`

Result SHA-256: `25ab75674fc68d32d963cd948d083e72c5e642e1274648aab6978fc227e8aa0d`

Exact verdict: `REFUTES_PERSISTABLE_LADDER_CROSS_SCALE_COHERENCE`.

The exact pre-frozen default-midpoint Persistable persistence-ladder architecture was activated only after the synthetic PASS in run `31954790384`. It used no shower truth and no protected/external scientific rows.

Against exact recurrent-EOM on the four frozen target-excluded GMN ~5.8k -> ~0.7k nested pairs:

- pooled symmetric mean-best-Jaccard: `0.4501830400` vs `0.4563513616`;
- median bucket symmetric score: `0.4348398011` vs `0.4711555852`;
- strict bucket wins: `2/4`;
- pooled fine->coarse: `0.5331517262` vs `0.6192052773`;
- pooled coarse->fine: `0.3672143538` vs `0.2934974460`.

The ladder improved the coarse->fine reverse direction but lost fine->coarse and total symmetric coherence. More importantly, the frozen fine-candidate non-collapse gate failed in all four buckets: the persistence ladder produced only `3/4/4/3` fine candidates while recurrent-EOM produced `9/5/6/9`.

Thus removing the global flat-cluster-count choice is not enough. This hierarchy remains too coarse at sparse GMN scale even though it passed the synthetic ladder audit.

The exact GEO6 + package-default midpoint slice + conservative g=2..15 membership-union architecture is closed. No result-informed rescue via representation, slice, ladder range, support, neighbor policy, flattening, subset, salt, comparator, coherence metric, candidate ceiling, or gates is authorized.

The strongest surviving structural clue remains the separately closed single-scale physical modal-basin diagnostic (#1279): its surviving family memberships were substantially more cross-scale coherent (`0.7962` pooled candidate mean vs `0.6153` recurrent-EOM) and won all four buckets, but it formally failed one candidate non-collapse gate. Any future work must be a genuinely distinct persistent/statistical modal architecture, not a MeanShift bandwidth rescue.

Protected `[20°,55°]`, OrbitTrace target information/events, shower truth, SonotaCo, ASFN/EFN event rows, AMOS, MAARSY and DMS were not accessed.