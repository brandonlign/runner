# Phase-equalized recurrent-EOM v1 — permanent no-rescue closure

Binding verdict:

`FAIL_PHASE_EQUALIZED_RECURRENT_EOM_V1_GMN_DEVELOPMENT`

This exact successor is closed permanently.

Do not use the observed GMN outcome to try any of the variants already prohibited before execution, including:

- changing the 55-degree arc origin;
- changing the 325-degree accessible-arc definition;
- per-year or equal-year phase CDFs;
- smoothed / binned / spline CDFs;
- partial equalization or raw/equalized phase blends;
- alternate tie rules or monotone phase transforms;
- HDBSCAN parameter changes;
- recurrent-EOM annual combiner/weight changes;
- rank fusion, reranking, fallback, or parent/successor union.

The observed loss of fixed-budget recovery cannot be used to choose a less aggressive phase transform. Any future successor must have a distinct independently motivated mechanism frozen before its first valid outcome.

Promoted recurrent-EOM HDBSCAN v1 remains the methodology parent.
