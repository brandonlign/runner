# OrbitTrace GMN v31 member-balance de-alias v1 — frozen protocol

## Status

**PRE-OUTCOME SCIENTIFIC FREEZE.** This defines one target-excluded GMN 2022/2023 offline correction to exact v31 before any corrected candidate score/rank/performance outcome is calculated.

Authoritative offline package: artifact `9167087908`; exact parent feature SHA-256 `fea3b063772c75b675e37a227b53a4aa3c5b86fdcbfcef1487b1e1448689cdf5`; exact parent OOF margin SHA-256 `f38c96e3fa4ea98f51217b36d639e96edbf3ebcb65123248f0f118d3298173bd`.

Parent fused controls: @25 `23`, @50 `41`, @100 `66`, top-100 dominant precision `0.7229521515453452`, MRR `0.050244164168646674`, qualified matches `95`.

No raw catalogue, SonotaCo, protected target region, OrbitTrace target information/events, MAARSY, or DMS access is required or authorized.

## Label-free representation audit

Before any candidate outcome, the exact 226x23 frozen feature matrix was inspected only for schema identity/redundancy. It established:

- column 8, structural `member_year_balance`, and column 12, cohesion `member_count_year_balance`, are bit-for-bit identical across all 226 rows;
- maximum absolute difference = `0.0`; differing rows = `0`;
- both describe the same annual member-count balance observable;
- columns 17–20 are also mutually identical but are constant zero across all rows, hence contribute exactly zero to the inherited standardized Euclidean geometry and are not changed by this successor.

No development label, rank outcome, candidate identity, or SonotaCo truth was used to identify the duplicate.

## Scientific motivation

Exact v31 fold-standardizes every coordinate and then uses ordinary Euclidean distance. Including the same nonconstant physical observable twice therefore gives that observable twice the squared-distance contribution of a singly represented observable. This is an accidental representation alias, not independent evidence.

The scientifically canonical correction is to retain the first occurrence and remove only the later duplicated occurrence. No weighting coefficient or feature search is introduced.

## Sole scientific change

Starting from exact frozen v31 23D matrix `X`, construct the candidate matrix by deleting exactly zero-based column `12`:

`X22 = np.delete(X, 12, axis=1)`.

Retain column 8 unchanged.

No other feature is removed, added, transformed, rescaled, or reordered. In particular, the four constant-zero neighbor-count columns remain present because they are inert and changing them would add no scientific information.

Candidate dimensionality is exactly 22.

## Exact inherited evaluator

Everything else remains exact v31:
- immutable 226 hard-family universe;
- exact strict whole-shower five-fold OOF split and binary development reference;
- fold-training mean / population-standard-deviation scaling, zero std -> `1.0`;
- ordinary Euclidean distance;
- k=1 nearest positive/nonpositive references;
- margin `d_nonpositive-d_positive`;
- exact diversity lambda `0.8`, scale `1.0`, immutable hard-order tie semantics;
- exact equal 1-based rank-sum fusion with immutable P19 hard order;
- exact monotone GMN evaluator.

Before the corrected 22D result is accepted, the evaluator must reproduce the exact 23D parent matrix SHA, parent margin SHA, hard control, and fused parent control.

## Binding PASS gate

First technically valid result is binding. PASS requires all:
1. recovered@100 > `66`;
2. recovered@25 >= `23`;
3. recovered@50 >= `41`;
4. top-100 dominant precision >= `0.7229521515453452`;
5. MRR >= `0.050244164168646674`;
6. qualified matches exactly `95`;
7. provenance/firewall checks pass.

A FAIL terminates this de-aliasing successor and does not authorize SonotaCo access.

## No-rescue closure

After the binding result do not retry:
- deleting column 8 instead of 12;
- averaging, halving, square-root weighting, or otherwise reweighting the two duplicate coordinates;
- removing the constant-zero columns or any additional feature;
- duplicate detection with tolerances/correlations rather than exact identity;
- removing any near-duplicate or correlated feature;
- feature subset search, PCA/SVD, whitening, covariance correction, metric/k/scaling changes;
- diversity/fusion changes, thresholds, rank windows, source rules, or identity-specific corrections;
- combining this correction with another successor.

Any future successor after a FAIL must change mechanism class and be independently motivated/frozen.

## Firewall

Scientific role: `TARGET_EXCLUDED_GMN_2022_2023_V31_OFFLINE_SUCCESSOR_DEVELOPMENT_ONLY`.

Binding calculation must record raw event rows accessed=false; SonotaCo 2013/2014 access=false; protected target-region events accessed=false; OrbitTrace target information access=false; MAARSY scientific access=false; DMS scientific access=false.
