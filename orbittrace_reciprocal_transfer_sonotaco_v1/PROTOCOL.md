# Reciprocal-transfer HDBSCAN v1 — dormant SonotaCo recurrent-parent benchmark

## Status

**Dormant pre-outcome protocol only.** Frozen before the first technically valid reciprocal-transfer HDBSCAN v1 GMN 2022/2023 development outcome is known.

This protocol must remain inactive unless the exact frozen reciprocal-transfer method returns `PASS_RECIPROCAL_TRANSFER_HDBSCAN_V1_GMN_DEVELOPMENT` under its target-excluded GMN gate. A GMN FAIL permanently leaves this protocol dormant and authorizes no SonotaCo execution or rescue.

SonotaCo 2013/2014 is **EXPOSED DEVELOPMENT ONLY**, not external validation. Protected OrbitTrace `[20.0,55.0]`, target information/events, AMOS, EFN, MAARSY, and DMS remain inaccessible.

## Frozen candidate method

Use exactly reciprocal-transfer HDBSCAN v1:

- protocol Git blob `6181ba8e4dfa34f869249389bda2eae46ca2c690`;
- kernel Git blob `f3a7c8d5ea53bf856fb8d0225d5d578c4248e5ce`;
- development runner Git blob `624b9d9855fa87e0e8331128af32eb88225d93cc`;
- two independent annual standard HDBSCAN fits in GEO6;
- `min_cluster_size=10`, `min_samples=10`, Euclidean, EOM, epsilon 0, no single cluster, prediction data enabled only for standard HDBSCAN transport;
- exact `hdbscan.prediction.approximate_predict` in both year directions;
- strict-majority counterpart requires one non-noise opposite-year label to receive strictly more than half of a native annual cluster's members;
- family exists only for reciprocal strict-majority mappings;
- membership is the union of the two exact native annual HDBSCAN member sets, with no transported-point additions;
- rank by descending worst-year native persistence, descending stronger-year persistence, descending worst-year native member count, descending total native member count, deterministic family ID.

Prediction probabilities and majority fractions remain reporting-only. No SonotaCo result may alter the majority rule, HDBSCAN parameters, prediction operator, membership rule, rank, or any other method byte.

## Frozen engineering prerequisites

A future activation requires the already-completed pre-GMN engineering evidence:

- synthetic audit run `31849167489`, artifact `9236929569`, digest `sha256:14181b33debe1a37e395d5a56bc8f3d461c356877e4bc6a64246ffb2abebe138`, result SHA-256 `8c39cb44258df6b8fbc3160dd2c2d2d98bc58de6910bda017cf6f726182cbea1`;
- source/firewall audit run `31849390666`, artifact `9237000627`, digest `sha256:8a8b87a8ed2a7c623d60d397ee6261f050184dae5170632a2424b10cc4644603`, result SHA-256 `930659eabef994d6977b5b38a92efab5b36ade64f22fa23e4d399376bb1aeeed`.

## Exact SonotaCo inputs

Use the exact immutable label-free preparation already used by promoted recurrent-EOM:

- artifact `9050107352`, `orbittrace-final-sonotaco-label-free-preparation-v2`;
- artifact digest `sha256:1296d757b5ea1dd94f9c9077fd769fdc8f00ec06d0881d8548fd1df4608344cc`.

For each matched route, fit one annual HDBSCAN model to 2013 rows and one to 2014 rows from exactly that route, then apply the frozen reciprocal-transfer construction.

### Sugar-matched route

- 2013 SHA-256 `47fb0b700fbf710c7b061eead343016bd8d182756eb0c7f406507c5739e4c4f8`, 18,638 events;
- 2014 SHA-256 `bc83c113e9a14b1c6e1ef460ca9a40e05df77f3a449fec6064f8910add04c912`, 15,400 events.

### HDBSCAN-matched route

- 2013 SHA-256 `2433b556d4a859580ef5431d2307ef34c8fa4c15d42841a2ec7b0c11e5f1f158`, 16,028 events;
- 2014 SHA-256 `206692292b2ca252777e40c13c367880740d8e2576d27615f7ea94b7790e3f55`, 13,283 events.

Only `id,year,sol,sun_lon,ecl_lat,vg` enter the method. Any protected `[20.0,55.0]` survivor is a technical no-result.

## Pretruth freeze

For each route, before any SonotaCo shower truth, recurrent-EOM result, v31 result, or literature score enters the execution process, persist and SHA-freeze:

- annual input event IDs and GEO6 hashes;
- both annual native HDBSCAN label vectors and persistence arrays;
- both annual condensed-tree hashes;
- complete 2013->2014 and 2014->2013 predicted-label vectors;
- prediction probabilities for provenance only;
- every strict-majority annual mapping or null status;
- every reciprocal pair;
- exact annual native memberships and pooled family memberships;
- the complete deterministic family ranking;
- source/runtime/prerequisite hashes.

No truth-bearing value may enter fitting, transport, matching, membership, or ranking.

## Exact evaluator and budgets

Only after the complete candidate payload is frozen, load the same immutable exposed truth/evaluation artifact used by promoted recurrent-EOM:

- artifact `9069505548`;
- exact `truth_{route}_{year}.json` and `evaluation_{route}_{year}.json` files;
- exact Hungarian maximum-F1 one-to-one assignment;
- shower eligibility threshold >=4 truth events;
- restrict pooled family members to the evaluated panel year without reranking;
- truncate to the existing comparator budget.

Frozen budgets:

- Sugar 2013: 34;
- Sugar 2014: 46;
- HDBSCAN 2013: 11;
- HDBSCAN 2014: 9.

## Exact promoted recurrent-EOM parent control

Promoted recurrent-EOM SonotaCo benchmark:

- run `31829200215`;
- artifact `9230008341`;
- artifact digest `sha256:a0eb8aafc88f3e963a3e788294f1a82bcc6612c26b587f5e12861a579486d110`;
- pretruth SHA-256 `c6afbc0c3443b6c34e3f90b0f63453a0a35bfae3f3c84ffe8a479f8f50cffeef`;
- result SHA-256 `c2395a86be5ba8a8b801210ac6e64b97c446e724991207aef85062ee00b89f12`.

Exact controls:

| Route | Year | Budget | recurrent-EOM macro-F1 | recurrent-EOM recovered F1>0.5 |
|---|---:|---:|---:|---:|
| Sugar | 2013 | 34 | 0.3752906816276458 | 23 |
| Sugar | 2014 | 46 | 0.43773122295664196 | 24 |
| HDBSCAN | 2013 | 11 | 0.1914598192215768 | 11 |
| HDBSCAN | 2014 | 9 | 0.1685878550176112 | 9 |

These values must be verified exactly before interpretation.

## Frozen parent-superiority gate

`PASS_RECIPROCAL_TRANSFER_SONOTACO_RECURRENT_PARENT_SUPERIORITY_V1` requires **all four** panels to satisfy both:

1. reciprocal-transfer macro-F1 is strictly greater than promoted recurrent-EOM macro-F1; and
2. reciprocal-transfer recovered F1>0.5 count is at least the promoted recurrent-EOM count.

No average, aggregate, route exception, or literature-only substitution is permitted. In particular HDBSCAN-2014 still requires macro-F1 strictly greater than `0.1685878550176112` while recovery remains at least 9.

A technically valid FAIL permanently rejects reciprocal-transfer v1 as a SonotaCo-superior replacement. No majority-threshold change, probability cutoff, centroid fallback, orphan pairing, alternate annual HDBSCAN parameters, ranking modification, or other SonotaCo-informed rescue is authorized.

## Outcome role and firewall

A PASS would remain exposed-development portability evidence, not external validation. AMOS remains the intended genuinely external survey.

Any future execution must assert:

- `scientific_role='EXPOSED_SONOTACO_PARENT_SUPERIORITY_DEVELOPMENT_ONLY'`;
- `sonotaco_role='EXPOSED_DEVELOPMENT_ONLY'`;
- `blind_exclusion=[20.0,55.0]`;
- `target_information_access=false`;
- `target_region_events_accessed=false`;
- `orbittrace_target_access=false`;
- `amos_scientific_access=false`;
- `efn_scientific_access=false`;
- `maarsy_scientific_access=false`;
- `dms_scientific_access=false`;
- `post_result_parameter_search=false`.
