# OrbitTrace v52 lexicographic minimax fusion v1

## Scientific role

Separately frozen exposed-development successor after the binding v51 consensus-bottleneck diagnostic PASS.

v51 established one specific HDB mechanism across the complete fixed 229-family universe: families recoverable in both exposed years tend to avoid a poor rank in either of the two already-frozen exact-v31 fusion inputs. The preregistered statistic was

`consensus_bottleneck = max(local_rank_percentile, v19_rank_percentile)`.

v52 translates only that supported mechanism into a parameter-free total order.

### Sugar

Sugar remains exact v31 byte-for-byte. v51 did not test Sugar, and exact v31 already wins both Sugar literature pair gates.

### HDB

Use the exact 229-family local and v19 ranks captured by the binding v51 vector. For each family define integer ranks

- `r_local` = exact-v31 local-geometry/diversity rank;
- `r_v19` = immutable exact-v19 rank.

The sole v52 HDB order is lexicographic ascending by

`(max(r_local, r_v19), min(r_local, r_v19), family_id)`.

This first minimizes the worse of the two exact-v31 input ranks; the better rank resolves only ties in the worst rank; family ID is the final deterministic tie-break. The rule is symmetric in the two inputs and contains no weight, coefficient, threshold, quantile, top-k, rank window, budget/year exception, or route-specific HDB subrule.

The frozen v51 vector contains the complete population and all ranks needed to construct this order without diagnostic recoverability labels or new SonotaCo outcome access.

SonotaCo 2013/2014 remains `EXPOSED_DEVELOPMENT_ONLY`, not external validation.

## Immutable authorizer and inputs

Before exposed truth is loaded, pin:

1. binding v51 run `31493423814`, artifact `9101972590`, artifact digest `sha256:56258a0be52d83c0d6dbfcffdb9fd9a2c6b73587ba8d92d7bdccdef9729868c9`;
2. v51 vector `V51_V31_CONSENSUS_BOTTLENECK_VECTOR.json`, SHA-256 `5f20a8bedb6e7b8d6c06d66e45d5037057a9853ded35a2b360333d6ea5e2c4cc`, canonical SHA-256 `0e13b3f9e6b791a13a3e90d853f8704573b1264dffcb67236e6423491ad70020`;
3. v51 diagnostic result SHA-256 `fef1d2c5bb83748de5c5511615915e76ce6fcd0801afbddb0e01bab09b9ab76d`, verdict `PASS_V51_V31_CONSENSUS_BOTTLENECK_DIAGNOSTIC`;
4. frozen parent v31 scientific source blob `917e3cd6f9310ca1282e0efa58ed0924d03ed4da`;
5. immutable #950 payload artifact `9074742322`, ZIP SHA-256 `d940fa255804866f14bc34b1d72467d17adddcfb7d82c954ed5a8d1668aa307a`;
6. immutable #839 ranker source from run `31344632499`, SHA-256 `dd14e899ac08c4081cfee7d2dac2e54d2f25f78427cc4bee30f30296cd24b990`;
7. immutable exposed SonotaCo truth artifact `9069505548`, ZIP SHA-256 `cdea3297c234b0b3a8f09c2208649c8607bb3e9a9004d299f6dcc18536ebb797`, loaded only after the complete v52 HDB order is frozen.

Require the v51 result to assert that no successor, minimax order, literature panel, threshold, alternate statistic, or rank-algebra search was evaluated.

## Pre-truth v52 HDB order freeze

Using only the frozen v51 vector:

1. require exactly 229 unique HDB family identities;
2. require each local and v19 rank to be a permutation of integers 1..229;
3. verify each stored normalized percentile equals `(rank-1)/228` and each stored bottleneck equals the maximum of those two percentiles;
4. reconstruct exact-v31 rank-sum order by `(r_local + r_v19, r_local, r_v19, family_id)` and require SHA-256 `85ac3f29a443c35b3812cf28c99ef13474fc3e0455458e20b41cec64d942073d`;
5. construct the sole v52 order by `(max(r_local,r_v19), min(r_local,r_v19), family_id)`;
6. require complete v52 HDB order SHA-256 `75fb44015e348c6b1bf0367e74db8e273e29862e132b5ef3305b2ddb409d8cc7`;
7. serialize the entire v31 and v52 HDB orders, per-family ranks, movement diagnostics, and canonical identity before exposed truth is restored.

Truth-blind structural audit fixed before outcome: the v52 order changes 217/229 global HDB positions relative to v31, with 117 moving up and 100 down, but changes exactly one membership in the first 9 and exactly one membership in the first 11. These prefix facts are consequences of the complete rule, not selected top-k rules and do not affect construction.

No annual own-family F1, recoverability label, literature panel outcome, component/quality/topology signal, boundary identity, target information, MAARSY, or DMS information may enter the v52 order freeze.

## Evaluation

Only after the complete v52 HDB order has been frozen may the workflow restore immutable exposed SonotaCo truth.

The evaluator must first reproduce exact v31 unchanged, including all four controls:

- Sugar 2013 `0.2719801488280529 / 16`;
- Sugar 2014 `0.31529041952487225 / 17`;
- HDB 2013 `0.14888037368183737 / 9`;
- HDB 2014 `0.15198123772301594 / 9`.

It must also reproduce the exact HDB local order SHA `9898a2ad69f251595b0de4ce5763ffb6641ad27141b6e54f839ebe240eb94595`, immutable v19 order SHA `e1e82ad70fb8c575ee7ee269906668931f07cbe3375c15ab84b0717b1f2c85dc`, and exact-v31 fused order SHA `85ac3f29a443c35b3812cf28c99ef13474fc3e0455458e20b41cec64d942073d`.

Sugar panels use exact-v31 order unchanged. HDB panels use exactly the pre-truth v52 order and the existing immutable literature budgets/evaluator.

PASS requires all four existing literature pair gates to win. The first technically valid v52 result is binding.

## Explicit prohibitions

No weighted maximum, soft maximum, average/mean/geometric/harmonic rank, rank product, alternative tie-break, v31-rank tie rescue, Pareto layer, threshold, quantile, top-k, rank window, budget/year rule, HDB subgroup exception, component/quality/topology signal, boundary identity, oracle family, diversity change, local-score change, v19 change, feature/model/k/metric/scaling change, fusion coefficient search, or post-result second search.

If v52 fails, this exact lexicographic minimax order is permanently closed. Do not rescue it with alternate tie rules, weighted maxima, soft-max, thresholds, or route/year exceptions.

## Firewall

Protected OrbitTrace solar longitude `20°–55°` remains inaccessible. Do not access OrbitTrace target information or target-region events. Do not access MAARSY or DMS scientifically. Every output must assert these conditions and `SonotaCo = EXPOSED_DEVELOPMENT_ONLY`.
