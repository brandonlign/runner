# Affine stream-tube source interface audit

This auxiliary runner-only workflow decodes the exact failed preflight source SHA-256 `e82eef021326cb5b570521064a6c78001cc5d0cac53fdb2e1e966362f3d4c1bb`, compiles it, and preserves its complete source plus static function/interface information.

It downloads no meteor data, reads no labels, forms no windows, computes no score, and accesses no GhostStream or confirmation panel. It creates no scientific result.

The sole purpose is to locate and correct the documented runtime interface error from workflow `30878027705`: the frozen source called nonexistent `scorer.build_complex_map`. Any corrected source must preserve every physical statistic, input, seed, calibration count, comparator, fold, threshold, gate, and blind interval.