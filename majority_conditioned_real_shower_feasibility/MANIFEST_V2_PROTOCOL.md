# Seven-year real-shower feasibility manifest v2

Manifest v1 remains killed before data access because it passed a 40-character Git blob identifier to `sha256sum`.

Manifest v2 changes exactly one integrity operation:

- fetch `real_shower_meta_stage0/audit_real_shower_data.py` from exact commit `bbd3eb514df2c0af7e8648ebc3ced5edbb7eec87`;
- compute its Git blob identifier with `git hash-object`;
- require exact blob `4a029051230f7c6e99b09e911f8a9e5228a58783`;
- record the file's SHA-256 only as provenance output after that immutable-blob check.

The seven official GMN years 2019–2025, every month, the exact parser source, prior MDC complex mapping, quality filters, 20°–55° blind interval, anonymous support definitions, gates, and no-score/no-identity boundary remain unchanged.

No event, source file, label, score, or GhostStream-region value was observed under manifest v1. If manifest v2 passes integrity, the same predeclared feasibility audit may execute once. Any scientific gate failure then kills this real-shower route without changing thresholds, years, or support rules.