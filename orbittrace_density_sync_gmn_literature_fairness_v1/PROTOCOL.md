# OrbitTrace density-synchronous recurrent-EOM — matched-capacity GMN literature fairness audit v1

## Status

**RETROSPECTIVE CHARACTERIZATION OF AN IMMUTABLE PRE-EXISTING METHOD.**

Density-synchronous recurrent-EOM HDBSCAN v1 was scientifically frozen and obtained its binding GMN development result in run `31852836840` before the later direct-GMN literature benchmark and matched-capacity recurrent-EOM fairness result existed. This audit therefore does not select, tune, rerank, or modify the method. It asks only what literature-comparison claim the already-frozen selected AMOS method actually supports.

The exact density-sync binding identities are:

- scientific execution head `182f07ade6bb5d4be2c80b88df9216bb2d6eee2d`;
- binding run `31852836840`;
- binding artifact `9238142199`, digest `sha256:918992863d019baf3bbb5eadd83ecaa32cabea3d9bd7d9d43735b26474e8ed60`;
- prelabel SHA-256 `efce0617a738a0372ec5b007fb87b46912accd8419f0f768829c4c0cb7d62993`;
- result SHA-256 `ca6aeed2b82739003ea5d39b59e869df876de2962164344a938fe4935ea38711`;
- verdict `PASS_DENSITY_SYNCHRONOUS_RECURRENT_EOM_V1_GMN_DEVELOPMENT`.

No density-sync scientific byte may change as a consequence of this audit.

## Question

At identical catalogue capacity, does the exact frozen density-synchronous recurrent-EOM catalogue outperform the tested published-method comparator catalogues on target-excluded GMN 2022 and 2023?

## Immutable comparator inputs

Reuse exactly the already-sealed direct-GMN literature benchmark:

- `GMN_LITERATURE_DIRECT_V1_PRETRUTH.json`, SHA-256 `f52371ba1a302d57a4050b380c2a744a3be560fee0916b28ba10efbdf20e8351`;
- `GMN_LITERATURE_DIRECT_V1_RESULT.json`, SHA-256 `20dd97323813f168da57383fe27dbd9685e68ddacd9b2ca1b9b31040c1cf1c4c`;
- exact direct literature evaluator blob `bb4dec3a40429db7d017bafcb442c536c4c7e6d6`;
- unchanged target-excluded GMN truth/runtime used by those comparator evaluations.

The tested literature routes are:

1. deterministic published Sugar-2017 DBSCAN core transfer;
2. published-configuration catalogue HDBSCAN-2025 transfer.

The Sugar claim boundary remains explicit: this GMN implementation represents the deterministic published DBSCAN core, not the full uncertainty-resampling Sugar pipeline.

## Matched-capacity rule

For comparator `m` and year `y`:

1. Let `K(m,y)` be the complete candidate count of the sealed literature comparator catalogue.
2. Evaluate the complete literature catalogue of exactly `K(m,y)` candidates.
3. Evaluate exactly the first `K(m,y)` candidates of the immutable density-sync catalogue; no membership or order changes are permitted.
4. Use the exact same annual eligible-shower truth and one-to-one Hungarian shower-F1 evaluator as the direct literature benchmark.
5. A pairwise win requires:
   - density-sync macro-F1 strictly greater than the comparator; and
   - density-sync recovered showers with assigned F1 > 0.5 at least the comparator count.

The four primary pairwise panels are Sugar 2022, Sugar 2023, HDBSCAN 2022, and HDBSCAN 2023.

A full audit PASS requires **4/4** pairwise wins.

## Binding outputs

Return exactly one overall token:

- `PASS_DENSITY_SYNC_GMN_MATCHED_CAPACITY_LITERATURE_4_OF_4`, or
- `NO_DENSITY_SYNC_GMN_MATCHED_CAPACITY_4_OF_4_SUPERIORITY`.

This audit result is characterization only. It cannot change method selection, reopen GMN method development, alter the already-frozen AMOS protocol, or authorize a result-informed successor.

## Claim boundary

A 4/4 PASS supports the statement that the exact frozen density-synchronous recurrent-EOM method outperforms the tested Sugar-core and catalogue-HDBSCAN implementations at matched candidate capacity on target-excluded GMN 2022/2023.

It does **not** establish universal superiority to all meteor-stream methods, the full uncertainty-resampled Sugar pipeline, or cross-survey external generalization. AMOS remains the sole planned pristine final external test; the historical negative ASFN result remains binding.

## Firewall and no-rescue rule

Inclusive solar longitude `[20.0,55.0]` remains excluded. OrbitTrace target information/events, SonotaCo, AMOS, ASFN/EFN event-level data, MAARSY, and DMS are not accessed by this audit.

No method change, reranking, threshold search, candidate-budget search, comparator retuning, panel selection, gate relaxation, result-informed rescue, or post-result parameter search is authorized. The first technically valid execution of this exact audit is binding.
