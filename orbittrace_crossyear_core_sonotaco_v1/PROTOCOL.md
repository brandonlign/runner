# Cross-year-core HDBSCAN v1 — dormant SonotaCo recurrent-parent benchmark

## Status

**Dormant pre-outcome protocol only.** Frozen before the first technically valid cross-year-core GMN 2022/2023 development outcome is known.

This protocol must remain inactive unless the exact frozen cross-year-core HDBSCAN v1 method returns `PASS_CROSSYEAR_CORE_HDBSCAN_V1_GMN_DEVELOPMENT` under its already-binding target-excluded GMN gate. A GMN FAIL permanently leaves this SonotaCo protocol dormant; it does not authorize execution or rescue.

SonotaCo 2013/2014 is **EXPOSED DEVELOPMENT ONLY**, never pristine external validation. The protected OrbitTrace interval `[20.0,55.0]`, OrbitTrace target information/events, AMOS, EFN, MAARSY, and DMS are inaccessible to this benchmark.

## Frozen candidate method

The candidate is exactly cross-year-core HDBSCAN v1 already frozen before its GMN outcome:

- scientific protocol Git blob `980d2f26d6477ff80d1b57606ee86ae96bebe972`;
- scalable Boruvka adapter Git blob `a7fec249d82fef23158ae52897ae28f9d0126153`;
- GEO6 representation unchanged;
- exactly two pooled years;
- for every event, `c_cross(x)` is distance to its 10th nearest GEO6 event in the other year;
- `k=10` inherited from promoted recurrent-EOM `min_samples=10`;
- mutual reachability `max(c_cross(i), c_cross(j), euclidean(i,j))`;
- exact pooled minimum spanning tree / single-linkage hierarchy;
- `min_cluster_size=10`;
- standard HDBSCAN condensation;
- annual normalized EOM values computed on the new hierarchy;
- recurrent stability `min(E_year1,E_year2)`;
- exact HDBSCAN EOM extraction using recurrent stability;
- ranking: recurrent stability descending, ordinary stability descending, member count descending, deterministic family ID.

No SonotaCo result may change k, representation, speed scale, metric, core-distance rule, MST rule, min cluster size, annual normalization, recurrent combiner, EOM extraction, candidate membership, ranking, or any other method byte.

## Frozen engineering prerequisites

Execution, if ever activated by a GMN PASS, must require the already-completed pre-GMN engineering evidence:

- dense-reference audit run `31846715847`, artifact `9236150866`, digest `sha256:dca493b29b9a5a1004fb621fddcfc17ae33ee6b71106d1878812e93b98ba1bea`;
- strict scalable-equivalence audit run `31847169035`, artifact `9236301386`, digest `sha256:0e62fceabca5ddf166a4ff62b303c2c70ba5b0a300f49e63becec20de48faf2d`, verdict `PASS_CROSSYEAR_CORE_BORUVKA_EXACTNESS_AUDIT_V1`, tolerance `1e-12`.

The strict audit existed before the first GMN workflow was activated.

## Exact SonotaCo label-free inputs

Use the exact immutable label-free preparation already used by promoted recurrent-EOM:

- artifact `9050107352`, `orbittrace-final-sonotaco-label-free-preparation-v2`;
- artifact digest `sha256:1296d757b5ea1dd94f9c9077fd769fdc8f00ec06d0881d8548fd1df4608344cc`.

Pool the exact two years within each established matched route before clustering:

### Sugar-matched route

- 2013 file SHA-256 `47fb0b700fbf710c7b061eead343016bd8d182756eb0c7f406507c5739e4c4f8`, 18,638 events;
- 2014 file SHA-256 `bc83c113e9a14b1c6e1ef460ca9a40e05df77f3a449fec6064f8910add04c912`, 15,400 events.

### HDBSCAN-matched route

- 2013 file SHA-256 `2433b556d4a859580ef5431d2307ef34c8fa4c15d42841a2ec7b0c11e5f1f158`, 16,028 events;
- 2014 file SHA-256 `206692292b2ca252777e40c13c367880740d8e2576d27615f7ea94b7790e3f55`, 13,283 events.

Only `id,year,sol,sun_lon,ecl_lat,vg` enter cross-year-core. Any protected `[20.0,55.0]` survivor is a technical no-result.

## Pretruth freeze

For each route, before any SonotaCo shower truth, literature result, v31 result, or recurrent-EOM benchmark result is loaded into the execution process, freeze and hash:

- exact input row IDs/hashes by year;
- all opposite-year 10-neighbor core distances and their hash;
- exact MST and single-linkage hierarchy hashes;
- condensed-tree hash;
- selected recurrent-EOM node set;
- every candidate membership and score;
- the complete deterministic pooled candidate order;
- source and prerequisite identities.

No truth-bearing or comparator-performance value may enter cross-year core construction, hierarchy, extraction, membership, or ranking.

## Exact evaluation semantics

Only after the complete candidate payload is persisted and SHA-256 frozen, load the same immutable exposed truth/evaluation artifact used by the promoted recurrent-EOM benchmark:

- artifact `9069505548`;
- exact `truth_{route}_{year}.json` and `evaluation_{route}_{year}.json` files;
- exact established Hungarian maximum-F1 one-to-one evaluation;
- eligible shower threshold: at least four truth events;
- restrict pooled candidate members to the panel year without reranking;
- truncate to the exact established comparator budget for that panel;
- report macro-F1 and number of assigned showers with F1 `>0.5`.

Frozen budgets remain:

- Sugar 2013: `34`;
- Sugar 2014: `46`;
- HDBSCAN 2013: `11`;
- HDBSCAN 2014: `9`.

## Exact promoted recurrent-EOM parent control

The binding promoted parent benchmark is immutable:

- run `31829200215`;
- artifact `9230008341`;
- artifact digest `sha256:a0eb8aafc88f3e963a3e788294f1a82bcc6612c26b587f5e12861a579486d110`;
- parent pretruth SHA-256 `c6afbc0c3443b6c34e3f90b0f63453a0a35bfae3f3c84ffe8a479f8f50cffeef`;
- parent result SHA-256 `c2395a86be5ba8a8b801210ac6e64b97c446e724991207aef85062ee00b89f12`.

Exact recurrent-EOM parent panel controls are:

| Route | Year | Budget | Parent macro-F1 | Parent recovered F1>0.5 |
|---|---:|---:|---:|---:|
| Sugar | 2013 | 34 | 0.3752906816276458 | 23 |
| Sugar | 2014 | 46 | 0.43773122295664196 | 24 |
| HDBSCAN | 2013 | 11 | 0.1914598192215768 | 11 |
| HDBSCAN | 2014 | 9 | 0.1685878550176112 | 9 |

These values must be byte/value verified before any successor interpretation.

## Frozen promotion gate

`PASS_CROSSYEAR_CORE_SONOTACO_RECURRENT_PARENT_SUPERIORITY_V1` requires **all four** panels to satisfy both:

1. cross-year-core macro-F1 is strictly greater than promoted recurrent-EOM macro-F1; and
2. cross-year-core recovered F1>0.5 count is at least the promoted recurrent-EOM count.

There is no averaging, route exception, aggregate rescue, or tie-break substitution. Thus the HDBSCAN-2014 panel still requires macro-F1 strictly above `0.1685878550176112` even though recovery may remain tied at 9.

Existing v31 and literature scores may be reported descriptively, but they cannot substitute for beating the promoted recurrent-EOM parent.

A technically valid FAIL permanently rejects cross-year-core v1 as a SonotaCo-superior replacement for recurrent-EOM v1. No SonotaCo-informed k change, core-distance blend, clipping, scaling, hierarchy change, ranking change, EOM change, feature addition, or other rescue is authorized.

## Outcome role

A PASS would establish that a method selected on target-excluded GMN also improves over the promoted recurrent-EOM parent across all four already-exposed SonotaCo portability panels. It remains development evidence, not external validation. AMOS remains the intended genuinely external survey validation route.

## Firewall declarations

Any future execution/result must assert:

- `scientific_role='EXPOSED_SONOTACO_PARENT_SUPERIORITY_DEVELOPMENT_ONLY'`;
- `sonotaco_role='EXPOSED_DEVELOPMENT_ONLY'`;
- `blind_exclusion=[20.0,55.0]`;
- `target_information_access=false`;
- `target_region_events_accessed=false`;
- `orbittrace_target_access=false`;
- `amos_scientific_access=false`;
- `efn_scientific_access=false`;
- `maarsy_scientific_access=false`;
- `dms_scientific_access=false`;
- `post_result_parameter_search=false`.
