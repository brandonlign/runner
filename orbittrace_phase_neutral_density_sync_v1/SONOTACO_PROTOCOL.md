# Phase-neutral density-sync v1 — dormant SonotaCo contingency

## Status

Frozen before the first paired GMN GEO6-vs-GEO4 outcome. Dormant unless the exact GMN protocol returns `PASS_PHASE_NEUTRAL_DENSITY_SYNC_V1_GMN_DEVELOPMENT`.

SonotaCo 2013/2014 is already exposed development/validation. This contingency is not external validation.

## Immutable exposed inputs

Use the exact existing label-free SonotaCo route rows from run `31354363306` and the exact already-exposed matched truth/evaluator artifact used by the recurrent-EOM benchmark. The protected inclusive `[20°,55°]` interval remains excluded.

Routes and years are fixed:

- Sugar 2013
- Sugar 2014
- published-catalogue-HDBSCAN 2013
- published-catalogue-HDBSCAN 2014

Established comparator budgets remain exactly 34, 46, 11 and 9 respectively.

## Methods

On each route, pool 2013+2014 label-free rows and fit two methods before truth is available:

1. **GEO6 density-sync champion** — exact six-dimensional representation, HDBSCAN settings and density-synchronous objective inherited from #1263;
2. **GEO4 phase-neutral density-sync successor** — identical except solar-longitude cosine/sine are absent from the clustering distance.

The complete hierarchy hashes, selected nodes, memberships and full order for both methods must be frozen before truth download.

No route-specific parameter or rule is allowed.

## One-shot portability gate

At each of the four established panels, evaluate both frozen candidate orders with the exact existing matched macro-F1 / recovered-shower evaluator and the established comparator budget.

The GEO4 successor passes only if:

- macro-F1 is not lower than the paired GEO6 density-sync champion on **all four** panels;
- recovered `F1 > 0.5` shower count is not lower on **all four** panels; and
- at least one panel improves strictly in macro-F1 or recovered count.

Because the GEO6 density-sync champion was previously shown to tie recurrent-EOM exactly on these four established panels, this paired gate also prevents degradation of the existing recurrent-EOM SonotaCo result.

## No rescue

After the first technically valid SonotaCo endpoint, no phase weighting, route-specific geometry, GEO4/GEO6 fusion, budget change, ranking change, HDBSCAN change, density-sync change or rerun is authorized.

A fail closes this exact successor. A pass supports exposed cross-catalogue portability only; it does not establish pristine external generalization.

## Firewall

OrbitTrace protected target information/events, AMOS, MAARSY and DMS remain inaccessible.