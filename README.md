# AMOS acquisition target

Fixed before data receipt: complete multi-station AMOS solutions for 2023 and 2024.

Requested columns: stable ID, UTC time/date, solar longitude, geocentric radiant, geocentric velocity, standard solution-quality flag. Request all solved meteors, not a shower-only subset. If shower associations exist, request them as a separate ID mapping.

First use after receipt is schema/capacity checking only. No year switching or method tuning based on received contents.