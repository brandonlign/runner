# Bivariate density component persistence v1 — binding result

## Verdict

🔴 **FAIL_BIVARIATE_DENSITY_COMPONENT_PERSISTENCE_V1 — CLOSED.**

Binding workflow run: `31965734456`

Binding workflow job: `95210516563`

Binding artifact: `9268506720` (`orbittrace-bivariate-density-component-persistence-v1`)

Binding artifact ZIP digest:

`sha256:8924a553ac8470a222681e604abd874184d3ad9abd4b3bff38fb82c1d9f58df4`

Immutable prelabel SHA-256:

`4b983662da83d2d0fee2196e82f154cb0f92fc1bfd77f85224ed957694c47404`

The repaired run reproduced both historical recurrent-EOM comparator implementations at the exact membership level, completed the exact two-annual-density threshold lattice, passed year-axis transpose and independent brute-force audits, sealed every candidate and rank, and only then opened shower truth. The result is the first technically valid outcome and is binding.

## Frozen method

The fixed #1284 physical radius-1 graph was retained. Annual local radius counts remained separate as `(d_22/N_22,d_23/N_23)` and were never scalarized. Every integer threshold pair `(k22,k23)` was enumerated; every induced connected-component membership was tracked over the complete two-parameter superlevel filtration. Support-4 memberships were ranked only by exact support-cell area `S(C)/(N_22*N_23)` and deterministic family hash.

## Aggregate truth result

### Fine sparse scale — denominator 1024 (~0.7k pooled events)

| Metric | recurrent-EOM | BDCP1 |
|---|---:|---:|
| qualified total | **20** | 12 |
| recovered @25 | **20** | 12 |
| recovered @50 | **20** | 12 |
| recovered @100 | **20** | 12 |
| recovered @500 | **20** | 12 |
| mean dominant precision | 0.3530315709574533 | **0.9639795821048485** |
| mean MRR | 0.6959325396825397 | **0.8229166666666666** |
| mean fragmentation | **1.0** | 4.3125 |

Panelwise qualified recovery: 2/8 nonlower, 1/8 strict wins, 6/8 losses.

### Coarse sparse scale — denominator 128 (~5.8k pooled events)

| Metric | recurrent-EOM | BDCP1 |
|---|---:|---:|
| qualified total | **94** | 16 |
| recovered @25 | **87** | 12 |
| recovered @50 | **94** | 16 |
| recovered @100 | **94** | 16 |
| recovered @500 | **94** | 16 |
| mean dominant precision | 0.3396191653933494 | **0.9444585517093576** |
| mean MRR | 0.23584530975502274 | **0.5270881042501576** |
| mean fragmentation | **1.0** | 16.8125 |

Panelwise qualified recovery: 0/8 nonlower, 0 strict wins, 8/8 losses.

## Frozen gates

Passed:

- fine mean MRR not lower;
- fine mean top-100 dominant precision not lower;
- coarse mean MRR not lower;
- coarse mean top-100 dominant precision not lower.

Failed:

- fine qualified total strictly greater;
- fine qualified nonlower in >=6/8 panels;
- fine fragmentation not higher;
- coarse qualified total not lower;
- coarse qualified nonlower in >=6/8 panels;
- coarse fragmentation not higher.

Thus the exact method passes 4/10 gates and fails promotion.

## Scientific interpretation

The full two-parameter annual-density partial order solves the *opposite* side of the current problem from pooled #1284: its first candidates are extremely pure and early (large MRR gains), but exact threshold-state memberships massively fragment physical streams and destroy recovery. At coarse scale fragmentation rises from `1.0` to `16.8125` while qualified recovery falls from `94` to `16`.

This means the missing method should not simply add more recurrence axes or rank exact multiparameter states. It needs a candidate topology that preserves the broad, stable physical families seen in #1284 while using a sample-size-stable evidence coordinate upstream. This exact two-parameter threshold lattice, support-area score, and exact-state candidate semantics are permanently closed. Do not rescue by threshold slices, Pareto subsets, area weights, component-size bonuses, scalar annual combiners, smoothing, or post-result fusion.

## Conditional SonotaCo transfer

The pre-frozen SonotaCo transfer protocol was not executed because the GMN promotion gate failed.

## Firewall

The inclusive protected solar-longitude interval `[20°,55°]` remained excluded. No OrbitTrace target information/events, SonotaCo event rows, ASFN/EFN event rows, AMOS scientific data, MAARSY, or DMS entered the GMN experiment.