# OrbitTrace v34 equal fusion of frozen v24 and v31 OOF orders v1

## Scientific role

Separately frozen exposed-SonotaCo development successor after the direct target/loss, leaf-support, leaf-weight, and covariance-geometry successors failed. Two already-frozen parent orders retain complementary HDBSCAN strengths under the same immutable candidate universe, memberships, strict whole-shower folds, diversity rule, v19 anchor, and evaluator: v24 reaches the full HDBSCAN-2013 recovery count (10/10 comparator count), while v31 reaches the full HDBSCAN-2014 recovery count (9/9) and misses HDBSCAN-2014 macro-F1 by only about 0.005. This successor tests one parameter-free consensus of those two complete final OOF orders and nothing else.

SonotaCo 2013/2014 remains exposed development-only.

## Frozen parent orders

Reconstruct both parents from the exact immutable #950/v24 71D pretruth payload and exact strict whole-shower folds:

1. **v24 parent**: exact two annual ExtraTrees regressors, annual score `min(pred_2013,pred_2014)`, exact #839 diversity `lambda=0.8, scale=1.0`, then exact equal rank-sum with frozen v19.
2. **v31 parent**: exact fold-local training mean/population-std standardization across all 71 dimensions; ordinary Euclidean k=1 nearest annual-positive (`F1_y>0.5`) and annual-nonpositive distances; margin `d_nonpositive-d_positive`; annual score `min(margin_2013,margin_2014)`; exact #839 diversity; then exact equal rank-sum with frozen v19.

Before the successor order is evaluated, the reconstructed parent orders must reproduce all eight exact parent panel results (four v24 and four v31). Failure to reproduce either parent is technical and yields no v34 result.

## Sole successor

For each route independently, take the complete frozen final v24 parent order and complete frozen final v31 parent order over the identical family universe. Produce exactly one new order by equal rank-sum:

`rank_v34(f) = rank_v24(f) + rank_v31(f)`

Sort ascending by that sum, then v24 rank, then v31 rank, then stable family ID. This is the exact deterministic ordering semantics of the already-used parameter-free rank-sum helper.

No third order is added. The v19 order is not fused again because it is already present identically inside both parent final orders. No weights, products, minimum/maximum rank, reciprocal rank, cutoff, route/year-specific choice, switching rule, parent selection, or second fusion is allowed.

Candidate memberships are unchanged. No additional diversity step follows the parent-order fusion because each parent already contains the same frozen #839 diversity stage; adding another diversity pass would be a second scientific change.

## Binding gate

The first technically valid execution is binding. PASS requires the sole v34 order to beat the corresponding literature comparator in all four Sugar/HDBSCAN 2013/2014 panels: macro-F1 strictly higher and recovered `F1>0.5` count at least equal in every panel.

Otherwise this exact equal fusion of frozen v24 and v31 final orders is a permanent no-go. Failure does not authorize fusion-weight search, alternate rank algebra, route/year switching, another diversity pass, parent modification, threshold/feature/model changes, membership changes, or post-result rescue.

A passing exposed-development order may be frozen only as an exposed SonotaCo development result; any claim of general superiority still requires separately preregistered protected validation.

No MAARSY, DMS, OrbitTrace target information, target-region event, or protected solar-longitude 20°–55° content may be accessed.